% ============================================================
% EXPOSÉ : THÉORIE DES DISTRIBUTIONS
% Design académique — noir/blanc/gris, corrections en annexe
% ============================================================
\documentclass[12pt,a4paper,openany]{book}

\usepackage{hyperref}



% ----- Encodage & langue -----
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[french]{babel}
\usepackage[nointegrals]{wasysym} 
\usepackage{graphicx} 



% ----- Police Palatino -----
\usepackage{mathpazo}

% ----- Mise en page -----
\usepackage{geometry}
\geometry{
	a4paper,
	top=3.5cm,
	bottom=3.0cm,
	left=2.5cm,
	right=2.5cm,
	headheight=14pt,
	headsep=0.5cm,
	footskip=1.2cm
}

% ----- Couleurs (noir/blanc/gris uniquement) -----
\usepackage{xcolor}
\definecolor{titlegray}{gray}{0.15}
\definecolor{theorembar}{gray}{0.45}
\definecolor{defbar}{gray}{0.15}
\definecolor{shadedef}{gray}{0.93}
\definecolor{shadethm}{gray}{0.96}
\definecolor{shaderem}{gray}{0.98}
\definecolor{shadeexo}{gray}{0.97}
\definecolor{shaderet}{gray}{0.91}


% ----- Microtypographie & espacement -----
\usepackage{microtype}
\usepackage{setspace}
\setstretch{1.2}
\clubpenalty=10000
\widowpenalty=10000

% ----- Mathématiques -----

\usepackage{amsmath, amssymb, amsthm}
\usepackage{mathrsfs}

% ----- En-têtes / pieds de page -----
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}

% Configuration des EN-TÊTES
\fancyhead[RO]{\small\itshape\color{titlegray}\leftmark}
\fancyhead[LE]{\small\itshape\color{titlegray}\rightmark}
\renewcommand{\headrulewidth}{0.4pt}

% Configuration des PIEDS DE PAGE
\renewcommand{\footrulewidth}{0.4pt} % Ajoute une ligne de séparation en bas (optionnel)

% Gauche (L) : Titre ou Info
\fancyfoot[LO,RE]{\small\color{titlegray} Théorie des Distributions} 

% Centre (C) : Votre nom ou autre
\fancyfoot[C]{\small\color{titlegray} ENSGMM - UNSTIM}

% Droite (R) : Numéro de page
\fancyfoot[RO,LE]{\bfseries\thepage}


\fancypagestyle{plain}{%
	\fancyhf{}%
	\renewcommand{\headrulewidth}{0pt}%
	\fancyfoot[C]{\thepage}%
}



% ----- Titres (chapitres, sections) -----
\usepackage{titlesec}
\titleformat{\chapter}[display]
{\normalfont\Large\color{titlegray}}
{\filleft\scshape\chaptertitlename~\thechapter}
{0pt}
{\titlerule[1pt]\vspace{8pt}\huge\bfseries\scshape\color{black}}
\titlespacing*{\chapter}{0pt}{-10pt}{40pt}

\titleformat{\section}
{\normalfont\large\bfseries\scshape\color{titlegray}}
{\thesection\quad}
{0pt}
{}
\titlespacing*{\section}{0pt}{1.5\baselineskip}{1\baselineskip}

\newcounter{exercice}[chapter]   % numérotation par chapitre : 1.1, 1.2, ...
\renewcommand{\theexercice}{\thechapter.\arabic{exercice}}

\titleformat{\subsection}
{\normalfont\normalsize\bfseries\color{titlegray}}
{\thesubsection\quad}
{0pt}
{}
\titlespacing*{\subsection}{0pt}{1\baselineskip}{0.5\baselineskip}

% ----- Environnements théorèmes (mdframed) -----
\usepackage[framemethod=TikZ]{mdframed}

% Style Définition : barre gauche noire, fond gris soutenu
\mdfdefinestyle{defstyle}{%
	linewidth=2.5pt, linecolor=defbar,
	topline=false, rightline=false, bottomline=false,
	innertopmargin=7pt, innerbottommargin=7pt,
	innerleftmargin=14pt, innerrightmargin=8pt,
	skipabove=12pt, skipbelow=12pt,
	backgroundcolor=shadedef,
	roundcorner=0pt,
}

% Style Théorème/Lemme/Proposition : barre gauche grise, fond très clair
\mdfdefinestyle{thmstyle}{%
	linewidth=2.5pt, linecolor=theorembar,
	topline=false, rightline=false, bottomline=false,
	innertopmargin=7pt, innerbottommargin=7pt,
	innerleftmargin=14pt, innerrightmargin=8pt,
	skipabove=12pt, skipbelow=12pt,
	backgroundcolor=shadethm,
	roundcorner=0pt,
}

% Style Remarque : fond blanc, cadre fin complet
\mdfdefinestyle{remstyle}{%
	linewidth=0.5pt, linecolor=black!30,
	innertopmargin=6pt, innerbottommargin=6pt,
	innerleftmargin=12pt, innerrightmargin=8pt,
	skipabove=8pt, skipbelow=8pt,
	backgroundcolor=shaderem,
}

\theoremstyle{definition}
\newmdtheoremenv[style=defstyle]{definition}{Définition}[chapter]

\theoremstyle{plain}
\newmdtheoremenv[style=thmstyle]{theorem}{Théorème}[chapter]
\newmdtheoremenv[style=thmstyle]{lemma}[theorem]{Lemme}
\newmdtheoremenv[style=thmstyle]{proposition}[theorem]{Proposition}
\newmdtheoremenv[style=thmstyle]{corollary}[theorem]{Corollaire}

\theoremstyle{remark}
\newtheorem*{remark}{Remarque}
\newtheorem*{example}{Exemple}
\newenvironment{remarque}{\begin{remark}}{\end{remark}}
\newenvironment{exemple}{\begin{example}}{\end{example}}

\renewcommand{\proofname}{\normalfont\itshape Démonstration}

% ----- Environnement QCM -----
\newenvironment{qcmblock}{%
	\begin{mdframed}[linewidth=0.5pt, linecolor=black!35,
		backgroundcolor=gray!5,
		skipabove=12pt, skipbelow=12pt,
		innerleftmargin=12pt, innerrightmargin=8pt,
		innertopmargin=8pt, innerbottommargin=8pt]%
		\noindent\textbf{\scshape QCM}\par\smallskip%
	}{%
	\end{mdframed}%
}

% ----- Environnement Exercice -----
\newenvironment{exerciceblock}[1]{%
	\refstepcounter{exercice}
	\begin{mdframed}[linewidth=2pt, linecolor=black!55,
		topline=false, rightline=false, bottomline=false,
		backgroundcolor=shadeexo,
		skipabove=12pt, skipbelow=6pt,
		innerleftmargin=14pt, innerrightmargin=8pt,
		innertopmargin=8pt, innerbottommargin=8pt]%
		\noindent\textbf{\scshape Exercice \theexercice}\ifx&#1&\else{} — \textit{#1}\fi\par\medskip%
	}{%
	\end{mdframed}%
}

% ----- Environnement À retenir -----
\newenvironment{retenir}[1]{%
	\begin{mdframed}[linewidth=3pt, linecolor=black!70,
		topline=false, rightline=false, bottomline=false,
		backgroundcolor=shaderet,
		skipabove=14pt, skipbelow=14pt,
		innerleftmargin=14pt, innerrightmargin=8pt,
		innertopmargin=8pt, innerbottommargin=8pt]%
		\noindent\textbf{\scshape À retenir}\ifx&#1&\else{} — #1\fi\par\smallskip%
	}{%
	\end{mdframed}%
}

% ----- Environnement Correction (annexe) -----
\newenvironment{correctionblock}[1]{%
	\begin{mdframed}[linewidth=0.5pt, linecolor=black!25,
		backgroundcolor=white,
		skipabove=10pt, skipbelow=10pt,
		innerleftmargin=12pt, innerrightmargin=8pt,
		innertopmargin=8pt, innerbottommargin=8pt]%
		\noindent\textbf{Correction} — \textit{#1}\par\medskip%
	}{%
	\end{mdframed}%
}

% ----- Navigation exercices <-> corrections -----
\newcommand{\navtoCorr}[2]{%
	\vspace{2pt}%
	\noindent\hfill{\small\itshape%
		\hyperlink{corr:#1:ex#2}{$\rightarrow$ Voir la correction, p.~\pageref*{corr:#1:ex#2}}%
	}%
	\vspace{6pt}%
}
\newcommand{\navtoExo}[2]{%
	\noindent{\small\itshape%
		\hyperlink{exo:#1:ex#2}{$\leftarrow$ Accéder à l'exercice, p.~\pageref*{exo:#1:ex#2}}%
	}\par\medskip%
}

% Navigation QCM <-> Réponse QCM
\newcommand{\navtoQCMRep}[2]{%
	\vspace{2pt}%
	\noindent\hfill{\small\itshape%
		\hyperlink{qcmrep:#1:q#2}{$\rightarrow$ Voir la réponse, p.~\pageref*{qcmrep:#1:q#2}}%
	}%
	\vspace{6pt}%
}
\newcommand{\navtoQCMchap}[1]{%
	\noindent{\small\itshape%
		\hyperlink{qcm:ch#1:start}{$\leftarrow$ Revenir aux questions du chapitre #1, p.~\pageref*{qcm:ch#1:start}}%
	}\par\medskip%
}

% ----- Listes -----
\usepackage{enumitem}
\setlist[itemize]{label=\textendash, leftmargin=1.5em}
\setlist[enumerate]{leftmargin=1.5em}

% ----- Tableaux professionnels -----
\usepackage{booktabs}
\usepackage{colortbl}
\usepackage{array}

% ----- Table des matières -----
\usepackage{tocloft}
\renewcommand{\cfttoctitlefont}{\hfill\huge\bfseries\scshape\color{titlegray}}
\renewcommand{\cftaftertoctitle}{\hfill}
\setlength{\cftbeforetoctitleskip}{0pt}
\setlength{\cftaftertoctitleskip}{1.5\baselineskip}
\renewcommand{\cftchapfont}{\bfseries\color{titlegray}}
\renewcommand{\cftchappagefont}{\bfseries}

% ----- Figures -----
\usepackage{caption}
\DeclareCaptionLabelFormat{sc}{\scshape #1 #2}
\captionsetup{labelformat=sc, labelsep=endash, font=small}
\renewcommand{\thefigure}{\thechapter.\arabic{figure}}

% ----- TikZ -----
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, patterns, decorations.pathmorphing, shadows , shadings, fadings}

\hypersetup{
	pdftitle={Théorie des Distributions},
	pdfauthor={BOTCHI Parfait},
	pdfsubject={Exposé de Méthode Mathématique de l'Ingénieur II (MMI 2)},
	pdfkeywords={Distributions, Dirac, UNSTIM, ENSGMM},
	pdfencoding=auto, 
	hidelinks
}

% ----- Hyperliens (tout en noir) -----
\usepackage{hyperref}
\hypersetup{
	colorlinks=true,
	linkcolor=black,
	citecolor=black,
	urlcolor=black,
	pdfborder={0 0 0},
}

% ----- Page de garde -----
\usepackage{pdfpages}

% ============================================================
\begin{document}
	\begin{titlepage}
		\thispagestyle{empty}
		
		\definecolor{unstimPrimary}{RGB}{0, 51, 102}
		\definecolor{unstimAccent}{RGB}{212, 175, 55}
		\definecolor{unstimGray}{RGB}{60, 60, 60}
		
		\begin{tikzpicture}[remember picture, overlay]
			
			%-----------------------------------------------------------
			% ARRIÈRE-PLAN MODERNISÉ : SYMÉTRIE DE COURBES LUXE
			%-----------------------------------------------------------
			\fill[white] (current page.south west) rectangle (current page.north east);
			
			% 1. COURBE SUPÉRIEURE (L'élément "Raide")
			%\shade[left color=unstimPrimary, right color=unstimPrimary!85, shading angle=135]
			%(current page.north west) -- ([xshift=14cm]current page.north west) 
			%.. controls ([xshift=10cm, yshift=-4cm]current page.north west) and ([xshift=0.5cm, yshift=-10cm]current page.north west) ..
			%([yshift=-18cm]current page.north west) -- cycle;
			
			% Filet doré supérieur
			%\draw[unstimAccent, line width=1.2pt, opacity=0.8]
			%([xshift=14.1cm]current page.north west) 
			%.. controls ([xshift=10.1cm, yshift=-4.1cm]current page.north west) and ([xshift=0.6cm, yshift=-10.1cm]current page.north west) ..
			%([yshift=-18.1cm]current page.north west);
			
			%-----------------------------------------------------------
			% 2. ÉLÉMENT INFÉRIEUR : TRILOGIE DE NAPPES DE FAISCEAUX
			%-----------------------------------------------------------
			\begin{scope}
				% --- NAPPE 1 : LE SOCLE (L'éventail large qui balaie le centre) ---
				\foreach \i in {0, 0.5, ..., 6} {
					\draw[unstimPrimary, opacity={0.02 + \i/100}, line width={0.1pt + \i*0.08pt}]
					([xshift={(-22 + \i*1.5)*1cm}]current page.south east) 
					.. controls ([xshift={(-14 + \i)*1cm}, yshift={(0.5 + \i*0.3)*1cm}]current page.south east) 
					and ([xshift={(-5 - \i*0.2)*1cm}, yshift={(6 + \i*0.4)*1cm}]current page.south east) .. 
					([yshift={(10 - \i*0.5)*1cm}]current page.south east);
				}
				
				% --- NAPPE 2 : L'ÉCLAT (Plus serré, doré, décalé vers le haut) ---
				\foreach \i in {0, 0.4, ..., 4} {
					\draw[unstimAccent, opacity={0.05 + \i/40}, line width={0.1pt + \i*0.1pt}]
					([xshift={(-18 + \i*1.2)*1cm}, yshift=0.5cm]current page.south east) 
					.. controls ([xshift={(-10 + \i)*1cm}, yshift={(3 + \i*0.4)*1cm}]current page.south east) 
					and ([xshift={(-2 - \i*0.1)*1cm}, yshift={(9 + \i*0.3)*1cm}]current page.south east) .. 
					([yshift={(14 - \i*0.4)*1cm}]current page.south east);
				}
				
				% --- NAPPE 3 : LE FILIGRANE (Très fin, très haut, bleu marine) ---
				\foreach \i in {0, 0.3, ..., 3} {
					\draw[unstimPrimary, opacity={0.1 + \i/15}, line width={0.2pt + \i*0.05pt}]
					([xshift={(-15 + \i*1)*1cm}, yshift=1cm]current page.south east) 
					.. controls ([xshift={(-7 + \i)*1cm}, yshift={(5 + \i*0.5)*1cm}]current page.south east) 
					and ([xshift={(-1 + \i*0.2)*1cm}, yshift={(11 + \i*0.2)*1cm}]current page.south east) .. 
					([yshift={(17 - \i*0.2)*1cm}]current page.south east);
				}
				\foreach \i in {0, 0.5, ..., 6} {
					\draw[unstimPrimary, opacity={0.03 + \i/30}, line width={0.1pt + \i*0.12pt}]
					([xshift={(-21 + \i*1.2)*1cm}]current page.south east) 
					% On pousse le premier contrôle très loin vers la gauche (-16cm) pour balayer le centre
					.. controls ([xshift={(-16 + \i)*1cm}, yshift={(2 + \i*0.5)*1cm}]current page.south east) 
					% Le deuxième contrôle reste à gauche (-10cm) pour donner de l'ampleur
					and ([xshift={(-10 - \i*0.5)*1cm}, yshift={(10 + \i)*1cm}]current page.south east) .. 
					% POINT FINAL : xshift=0pt pour toucher le bord droit de la page
					([xshift=0pt, yshift={(15 - \i*0.8)*1cm}]current page.south east);
				}
				
				
				
				% --- LIGNE MAÎTRESSE ET POINTS (La signature) ---
				\draw[unstimAccent, line width=1.4pt, opacity=0.7]
				([xshift=-15cm, yshift=1cm]current page.south east) 
				.. controls ([xshift=-7cm, yshift=5cm]current page.south east) 
				and ([xshift=-1cm, yshift=11cm]current page.south east) .. 
				([yshift=17cm]current page.south east);
				
				\foreach \p in {0.25, 0.5, 0.75} {
					\path ([xshift=-15cm, yshift=1cm]current page.south east) 
					.. controls ([xshift=-7cm, yshift=5cm]current page.south east) 
					and ([xshift=-1cm, yshift=11cm]current page.south east) .. 
					([yshift=17cm]current page.south east) 
					node[pos=\p, circle, fill=white, draw=unstimAccent, line width=0.6pt, inner sep=1.2pt] {};
				}
			\end{scope}
			
			
			
			% Filet doré inférieur
			\draw[unstimAccent, line width=1pt, opacity=0.7]
			([yshift=8.1cm]current page.south east)
			.. controls ([xshift=-4.1cm, yshift=4.1cm]current page.south east) and ([xshift=-10.1cm, yshift=0.6cm]current page.south east) ..
			([xshift=-14.1cm]current page.south east);
			
			% 3. MOTIFS DE DISPERSION (Version ultra-robuste)
			\foreach \i in {1,...,20} {
				\fill[unstimAccent, opacity=0.3] 
				([xshift={(15*rnd)*1cm}, yshift={(-12*rnd)*1cm}]current page.north west) circle (1pt);
				\fill[unstimPrimary!40, opacity=0.2] 
				([xshift={(-15*rnd)*1cm}, yshift={(10*rnd)*1cm}]current page.south east) circle (0.8pt);
			}
			
			
			
			% 4. ÉLÉMENT TECHNIQUE DISCRET (Haut-Droite)
			\foreach \r in {0.8, 1.3, ..., 3} {
				\draw[unstimPrimary!15, line width=0.2pt] 
				([xshift=-1.9cm, yshift=-2.3cm]current page.north east) circle (\r cm);
			}
			
			
			%-----------------------------------------------------------
			% LOGOS ET ÉTABLISSEMENTS
			%-----------------------------------------------------------
			
			\node[anchor=north west, inner sep=0]
			at ([xshift=0.7cm, yshift=-1.3cm]current page.north west)
			{\includegraphics[height=2.3cm]{logo_unstim.png}};
			
			\node[anchor=north west, font=\bfseries\large\color{unstimPrimary}]
			at ([xshift=3.1cm, yshift=-1.1cm]current page.north west)
			{\textsc{UNSTIM}};
			\node[anchor=north west, font=\fontsize{13pt}{15pt}\selectfont\color{unstimGray}, text width=7cm]
			at ([xshift=3.1cm, yshift=-1.9cm]current page.north west)
			{Université Nationale des Sciences,
				Technologies, Ingénierie \\
				et Mathématiques};
			
			\node[anchor=north east, inner sep=0]
			at ([xshift=-0.7cm, yshift=-1.3cm]current page.north east)
			{\includegraphics[height=2.1cm]{logo_ensgmm.png}};
			
			\node[anchor=north east, font=\bfseries\large\color{unstimPrimary}]
			at ([xshift=-3.1cm, yshift=-1.1cm]current page.north east)
			{\textsc{ENSGMM}};
			\node[anchor=north east, font=\fontsize{13pt}{15pt}\selectfont\color{unstimGray},
			text width=7.8cm, align=right]
			at ([xshift=-3.1cm, yshift=-1.9cm]current page.north east)
			{École Nationale Supérieure \\
				de Génie
				Mathématique \\ et Modélisation};
			%-----------------------------------------------------------
			% NOM DE LA MATIÈRE (AGRANDI ET CONTRASTÉ)
			%-----------------------------------------------------------
			\node[anchor=north, align=center, font=\fontsize{20pt}{15pt}\selectfont]
			at ([ yshift=-6.6cm]current page.north)
			{\color{unstimPrimary}  MMI 2 --  Méthodes Mathématiques de l'Ingénieur II};
			
			%-----------------------------------------------------------
			% BLOC TITRE 
			%-----------------------------------------------------------
			\begin{scope}
				% 1. OMBRE PORTÉE 
				\fill[black!15, rounded corners=12pt]
				([xshift=1.7cm, yshift=-8.2cm]current page.north west) rectangle
				([xshift=-1.3cm, yshift=-13.2cm]current page.north east);
				
				% 2. CORPS DU BLOC 
				\filldraw[white, draw=unstimPrimary!20, line width=0.5pt]
				([xshift=1.5cm, yshift=-8.0cm]current page.north west) [rounded corners=12pt] --
				([xshift=-1.5cm, yshift=-8.0cm]current page.north east) --
				([xshift=-1.5cm, yshift=-11.8cm]current page.north east) -- 
				([xshift=-2.7cm, yshift=-13.0cm]current page.north east) [rounded corners=12pt] -- 
				([xshift=1.5cm, yshift=-13.0cm]current page.north west) -- cycle;
				
				% 3. LE RABAT DU PLI 
				\shade[left color=gray!10, right color=gray!30, shading angle=45, draw=unstimPrimary!30]
				([xshift=-1.5cm, yshift=-11.8cm]current page.north east) -- 
				([xshift=-2.7cm, yshift=-13.0cm]current page.north east) -- 
				([xshift=-2.7cm, yshift=-11.8cm]current page.north east) -- cycle;
				
				% 4. OMBRE DE CONTACT 
				\fill[black!20]
				([xshift=-1.5cm, yshift=-11.8cm]current page.north east) -- 
				([xshift=-2.7cm, yshift=-13.0cm]current page.north east) -- 
				([xshift=-1.9cm, yshift=-12.1cm]current page.north east) -- cycle;
			\end{scope}
			
			%-----------------------------------------------------------
			% TEXTE DU BLOC 
			%-----------------------------------------------------------
			\node[anchor=north, align=center, font=\large\itshape\bfseries\color{unstimGray}]
			at ([yshift=-8.6cm]current page.north) {Thème :};
			
			\node[anchor=north, align=center, font=\bfseries\huge\color{unstimPrimary}]
			at ([yshift=-9.7cm]current page.north) {\textsc{Théorie des Distributions}};
			
	
\draw[unstimAccent, line width=1.5pt]
    ([xshift=-3cm, yshift=-10.9cm]current page.north) -- 
    node[midway, fill=white, inner sep=1pt] {\rotatebox{45}{$\wasylozenge$}} 
    ([xshift=3cm, yshift=-10.9cm]current page.north);


			
			\node[anchor=north, align=center, font=\normalsize\scshape\bfseries\color{unstimPrimary!80}]
			at ([yshift=-12.1cm]current page.north) {Ingénieur 1 --- Génie Mathématique et Modélisation};
			%-----------------------------------------------------------
			% ENSEIGNANT RESPONSABLE (DÉCALÉ VERS LE BAS)
			%-----------------------------------------------------------
			\node[anchor=north, align=center, font=\small\color{unstimGray}]
			at ([yshift=-14.5cm]current page.north)
			{Sous la direction de};
			
			\node[anchor=north, align=center, font=\bfseries\Large\color{unstimPrimary}]
			at ([yshift=-15.3cm]current page.north)
			{Dr.\ MOUSSA DJIBRIL A.\ };
			
			\draw[unstimAccent, line width=1pt]
			([xshift=-2.5cm, yshift=-16.4cm]current page.north) --
			([xshift=2.5cm, yshift=-16.4cm]current page.north);
			
			
			%-----------------------------------------------------------
			% MEMBRES DU GROUPE 
			%-----------------------------------------------------------
			
			% Titre du bloc
			\node[anchor=north, font=\fontsize{16pt}{15pt}\selectfont\color{unstimPrimary}] 
			at ([yshift=-18.5cm]current page.north) 
			{\textsc{\textbf{Membres du Groupe}}};
			
			% Le Tableau 
			\node[anchor=north] at ([yshift=-19.8cm]current page.north) {
				\begin{tikzpicture}
					% Fond du tableau (Gris ultra-léger avec bordure latérale)
					\fill[unstimGray!5, rounded corners=2pt] (0,0) rectangle (14,2.5);
					\fill[unstimPrimary] (0,0) rectangle (0.15,2.5); % Barre bleue à gauche
					\fill[unstimAccent] (13.85,0) rectangle (14,2.5); % Barre dorée à droite
					
					% Contenu du tableau
					\node[anchor=west] at (0.8, 1.8) {\large\color{unstimGray} \textbf{01.} \textsc{Affoukou} Prosper};
					\node[anchor=west] at (7.5, 1.8) {\large\color{unstimGray} \textbf{02.} \textsc{Botchi} Parfait};
					
					% Ligne de séparation interne ultra-fine
					\draw[unstimGray!20] (0.8, 1.25) -- (13.2, 1.25);
					
					\node[anchor=west] at (0.8, 0.7) {\large\color{unstimGray} \textbf{03.} \textsc{Doboevi} Judicaël Karol};
					\node[anchor=west] at (7.5, 0.7) {\large\color{unstimGray} \textbf{04.} \textsc{Dohou} Alexis};
				\end{tikzpicture}
			};
			
			
			%-----------------------------------------------------------
			% PIED DE PAGE
			%-----------------------------------------------------------
			
			
			\foreach \i in {0, 0.5, 1, ..., 10} {
				\fill[unstimPrimary, opacity={0.3 - 0.025*\i}]
				([xshift={2cm + 1.8cm*\i}, yshift=1.2cm]current page.south west)
				circle (1.5pt);
			}
			
			\node[anchor=south, align=center, font=\large\color{unstimGray}]
			at ([yshift=1.5cm]current page.south)
			{ Année universitaire 2025--2026};
			
		\end{tikzpicture}
	\end{titlepage}
	
	%\cleardoublepage
	\pagestyle{empty}
	\setcounter{page}{1}
	

	% ==================== TABLE DES MATIÈRES ====================
	\frontmatter
	\tableofcontents
	
	 
	\listoffigures
	\addcontentsline{toc}{chapter}{Table des figures}
	\pagestyle{fancy}
	
	\mainmatter
	
	% ============================================================
	% CONTEXTE
	% ============================================================
	\chapter*{Contexte}
	\addcontentsline{toc}{chapter}{Contexte}
	\markboth{CONTEXTE}{CONTEXTE}
	
	\section*{Contexte}
	
	La théorie des distributions a été développée par le mathématicien français Laurent Schwartz dans les années 1940–1950, travail pour lequel il a reçu la médaille Fields en 1950. L'objectif était de donner un cadre rigoureux à des objets qui apparaissaient naturellement en physique — notamment l'impulsion de Dirac $\delta$, utilisée par le physicien Paul Dirac depuis les années 1930 sans justification mathématique satisfaisante.
	
	Le problème central que résout cette théorie est le suivant : certaines fonctions physiquement naturelles (la fonction de Heaviside, les solutions de certaines équations aux dérivées partielles) ne sont pas dérivables au sens classique. L'idée de Schwartz est de renverser le point de vue : plutôt que d'évaluer une fonction en un point, on mesure son effet sur des fonctions très régulières appelées fonctions test. Cela permet de dériver n'importe quel objet raisonnable, et de donner un sens à des équations qui n'en avaient pas.
	\newpage
	\subsection*{Notations — valables pour tout l'exposé}
	
	Dans tout ce qui suit, $\Omega$ désigne un ouvert de $\mathbb{R}^d$ ($d \ge 1$), et $k \in \mathbb{N} \cup \{\infty\}$.
	\begin{center}
		\renewcommand{\arraystretch}{1.4}
		\begin{tabular}{@{}cl@{}}
			\toprule
			\textbf{Notation} & \textbf{Signification} \\
			\midrule
			$C^{k}(\Omega)$ & Fonctions $k$ fois dérivables à dérivées d'ordre $k$ continues sur $\Omega$ \\
			$C^{\infty}(\Omega)$ & Fonctions infiniment dérivables sur $\Omega$ \\
			$C_{c}^{\infty}(\Omega)=\mathcal{D}(\Omega)$ & Fonctions $C^{\infty}$ à support compact inclus dans $\Omega$ \\
			$\mathcal{D}^{\prime}(\Omega)$ & Espace des distributions sur $\Omega$ \\
			$\operatorname{supp}(f)$ & Support de $f$ : adhérence de $\{x \in \Omega \mid f(x) \neq 0\}$ \\
			$\langle T, \varphi\rangle$ & Crochet de dualité : action de la distribution $T$ sur $\varphi$ \\
			$L_{\text{loc}}^{1}(\Omega)$ & Fonctions localement intégrables sur $\Omega$ \\
			$\alpha \in \mathbb{N}^{d}$ & Multi-indice : $\alpha = (\alpha_1, \dots, \alpha_d)$, $|\alpha| = \alpha_1 + \cdots + \alpha_d$ \\
			$\partial^{\alpha}f$ & Dérivée partielle $\frac{\partial^{|\alpha|}f}{\partial x_1^{\alpha_1} \cdots \partial x_d^{\alpha_d}}$ \\
			$\partial^{\alpha}T$ & Dérivée d'ordre $\alpha$ de la distribution $T$ \\
			$T^{(n)}$ & Dérivée d'ordre $n$ de $T$ en dimension $1$ \\
			$H$ & Fonction de Heaviside : $H(x) = 1$ si $x > 0$, $0$ sinon \\
			$\operatorname{sgn}$ & Fonction signe : $\operatorname{sgn}(x) = 1$ si $x > 0$, $-1$ si $x < 0$ \\
			$\delta_{a}$ & Distribution de Dirac au point $a$ : $\langle \delta_{a}, \varphi \rangle = \varphi(a)$ \\
			\bottomrule
		\end{tabular}
	\end{center}
	
	
	
	
	
	
	% ============================================================
	% CHAPITRE 1 : NOTION DE DISTRIBUTION
	% ============================================================
	
	\chapter{Notion de distribution}
	

	
	\section{L'espace des fonctions test}
	
	\begin{definition}[Fonctions test — $\mathcal{D}(\Omega)$]
		L'espace des fonctions test est
		\[
		\mathcal{D}(\Omega) = C_c^{\infty}(\Omega) = \{ \varphi \in C^{\infty}(\Omega) \mid \operatorname{supp}(\varphi) \text{ est compact et contenu dans } \Omega \}.
		\]
	\end{definition}
	
	\textbf{Intuition.} Une fonction test est infiniment régulière et s'annule au-delà d'un certain rayon. On peut la voir comme une « sonde » qu'on applique à un objet mathématique pour mesurer ses propriétés locales sans perturber ce qui se passe loin.
	
	\begin{definition}[Convergence dans $\mathcal{D}(\Omega)$]
		Une suite $(\varphi_n) \subset \mathcal{D}(\Omega)$ converge vers $\varphi$ dans $\mathcal{D}(\Omega)$ si :
		\begin{enumerate}
			\item[(i)] Il existe un compact $K \subset \Omega$ tel que $\operatorname{supp}(\varphi_n) \subset K$ pour tout $n$.
			\item[(ii)] Pour tout multi-indice $\alpha \in \mathbb{N}^d$, $\partial^\alpha \varphi_n \to \partial^\alpha \varphi$ uniformément sur $K$.
		\end{enumerate}
	\end{definition}
	
	\section{Définition d'une distribution}
	
	Il existe deux formulations équivalentes, que l'on présente toutes les deux.
	
	\begin{definition}[Distribution — définition séquentielle]
		Une distribution sur $\Omega$ est une forme linéaire $T : \mathcal{D}(\Omega) \to \mathbb{R}$ telle que :
		\[
		\varphi_n \xrightarrow{\mathcal{D}(\Omega)} \varphi \implies \langle T, \varphi_n \rangle \to \langle T, \varphi \rangle \quad \text{dans } \mathbb{R}.
		\]
	\end{definition}
	
	\begin{definition}[Distribution — définition par majoration]
		Une forme linéaire $T : \mathcal{D}(\Omega) \to \mathbb{R}$ est une distribution si et seulement si, pour tout compact $K \subset \Omega$, il existe $C_K > 0$ et $m \in \mathbb{N}$ tels que
		\[
		|\langle T, \varphi \rangle| \le C_K \max_{|\alpha| \le m} \sup_{x \in K} |\partial^\alpha \varphi(x)|, \quad \forall \varphi \in \mathcal{D}(\Omega) \text{ avec } \operatorname{supp}(\varphi) \subset K.
		\]
		L'entier $m$ s'appelle l'\textbf{ordre} de la distribution. Si $m$ peut être choisi indépendant de $K$, on dit que $T$ est d'ordre $m$.
	\end{definition}
	
	L'ensemble de toutes les distributions sur $\Omega$ se note $\mathcal{D}'(\Omega)$.
	
	\section{Les exemples fondamentaux}
	
	\begin{example}[Distribution régulière]
		Toute fonction $f \in L_{\text{loc}}^1(\Omega)$ (localement intégrable) définit une distribution $T_f$ via
		\[
		\langle T_f, \varphi \rangle = \int_{\Omega} f(x) \varphi(x) \, dx, \quad \forall \varphi \in \mathcal{D}(\Omega).
		\]
		La majoration $|\langle T_f, \varphi \rangle| \le \|f\|_{L^1(K)} \|\varphi\|_\infty$ montre que $T_f$ est d'ordre $0$. On dit que $f$ est identifiée à $T_f$, et que $T_f$ est une distribution régulière. Ainsi les fonctions classiques sont des distributions, mais pas l'inverse.
	\end{example}
	
	\begin{example}[La distribution de Dirac $\delta_a$]
		Pour $a \in \Omega$, le Dirac en $a$ est défini par
		\[
		\langle \delta_a, \varphi \rangle = \varphi(a), \quad \forall \varphi \in \mathcal{D}(\Omega).
		\]
		Vérification rapide (majoration) :
		\[
		|\langle \delta_a, \varphi \rangle| = |\varphi(a)| \le \sup_{x \in K} |\varphi(x)|,
		\]
		ce qui correspond à la majoration avec $C_K = 1$ et $m = 0$. Donc $\delta_a \in \mathcal{D}'$, d'ordre $0$.
		
		\textbf{Attention.} $\delta_a$ n'est pas une distribution régulière : il n'existe aucune fonction $f$ telle que $\int_\Omega f(x)\varphi(x)\,dx = \varphi(a)$ pour toute $\varphi \in \mathcal{D}(\Omega)$.
	\end{example}
	
	\begin{figure}[htbp]
		\centering
		\begin{tikzpicture}[scale=1.2,
			axisstyle/.style={-Stealth, semithick, black},
			arrowstyle/.style={-{Stealth[length=5pt, width=4pt]}, thick, black}]
			
			% Axes
			\draw[axisstyle] (-2.5,0) -- (2.5,0) node[right] {$x$};
			\draw[axisstyle] (0,-0.3) -- (0,2.5) node[above] {$y$};
			
			% Fonction de Heaviside H(x)
			\draw[thick, black!70] (-2.4,0) -- (0,0);
			\draw[thick, black!70] (0,1.5) -- (2.4,1.5);
			% Point d'exclusion en 0 (cercle vide)
			\filldraw[white, draw=black!70, thick] (0,0) circle (2.5pt);
			% Point d'inclusion en 0 pour la valeur 1 (cercle plein)
			\filldraw[black!70] (0,1.5) circle (2.5pt);
			
			% Étiquette H(x)
			\node[font=\small\itshape] at (-1.2,0.6) {$H(x)$};
			
			% Flèche représentant la masse de Dirac δ₀
			\draw[arrowstyle] (0,1.2) -- (0,0.2);
			\node[above right, font=\small] at (0,1.2) {$\delta_0$};
			
			% Optionnel : ligne pointillée pour relier visuellement
			\draw[gray, densely dotted, thin] (0,0) -- (0,1.5);
		\end{tikzpicture}
		\caption{Représentation symbolique de la fonction de Heaviside $H(x)$ et de la masse de Dirac $\delta_0$ (flèche).}
		\label{fig:heaviside_dirac}
	\end{figure}
	
	\begin{example}[La fonction de Heaviside]
		La fonction de Heaviside $H : \mathbb{R} \to \mathbb{R}$, définie par $H(x)=1$ si $x>0$ et $H(x)=0$ sinon, est dans $L_{\text{loc}}^1(\mathbb{R})$. Elle définit donc une distribution régulière :
		\[
		\langle T_H, \varphi \rangle = \int_0^{+\infty} \varphi(x) \, dx.
		\]
		Elle n'est pas dérivable en $0$ au sens classique — c'est précisément ce genre de situation que la théorie des distributions permet de dépasser.
	\end{example}
	
	%----------------------------------------------------------
	\begin{qcmblock}
		\hypertarget{qcm:ch1:start}{}\label{qcm:ch1:start}
		\hypertarget{qcm:ch1:q1}{}\label{qcm:ch1:q1}
		\textbf{Question 1.} Parmi les affirmations suivantes, laquelle est vraie ?
		\begin{enumerate}[label=\Alph*., itemsep=2pt]
			\item Toute distribution est une fonction.
			\item Toute fonction localement intégrable est une distribution.
			\item La distribution de Dirac est une distribution régulière.
			\item L'espace $\mathcal{D}(\Omega)$ contient toutes les fonctions continues.
		\end{enumerate}
		\navtoQCMRep{ch1}{1}
		
		\medskip
		\hypertarget{qcm:ch1:q2}{}\label{qcm:ch1:q2}
		\textbf{Question 2.} Soit $\varphi \in \mathcal{D}(\mathbb{R})$ avec $\varphi(2) = -3$ et $\varphi(0) = 7$. Calculer $\langle \delta_2, \varphi \rangle$.
		\begin{enumerate}[label=\Alph*., itemsep=2pt]
			\item $7$ \quad B. $-3$ \quad C. $0$ \quad D. $\varphi'(2)$
		\end{enumerate}
		\navtoQCMRep{ch1}{2}
	\end{qcmblock}
	%----------------------------------------------------------
	\begin{exerciceblock}{Vérifier qu'une forme est une distribution}
		\hypertarget{exo:ch1:ex1}{}\label{exo:ch1:ex1}
		
		Soit $T : \mathcal{D}(\mathbb{R}) \to \mathbb{R}$ définie par
		\[
		\langle T, \varphi \rangle = 3\varphi(0) - \varphi(1).
		\]
		
		\begin{enumerate}
			\item Vérifier que $T$ est linéaire.
			\item Vérifier que $T$ est une distribution en utilisant la définition par majoration.
			\item Quel est l'ordre de $T$ ?
			\item Exprimer $T$ comme combinaison de distributions connues.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch1}{1}
	
	%----------------------------------------------------------
	\begin{retenir}{Chapitre 1}
		\begin{itemize}
			\item $\mathcal{D}(\Omega) = C_c^\infty(\Omega)$ : fonctions $C^\infty$ à support compact — ce sont les « sondes ».
			\item Une distribution est une forme linéaire continue sur $\mathcal{D}(\Omega)$ (continuité = séquentielle = contrôle par les dérivées de $\varphi$).
			\item Les fonctions $L_{\text{loc}}^1$ définissent des distributions régulières ; le Dirac est une distribution, mais singulière (pas associée à une fonction).
			\item L'ordre mesure combien de dérivées de $\varphi$ sont nécessaires dans la majoration.
		\end{itemize}
	\end{retenir}
	
	% ============================================================
	% CHAPITRE 2 : CONVERGENCE DANS D'(Ω)
	% ============================================================
	
	\chapter{Convergence dans $\mathcal{D}'(\Omega)$}
	
	\section{Définition}
	
	\begin{definition}[Convergence dans $\mathcal{D}'(\Omega)$]
		Une suite $(T_n)$ de distributions converge vers $T$ dans $\mathcal{D}'(\Omega)$ si
		\[
		\forall \varphi \in \mathcal{D}(\Omega), \quad \langle T_n, \varphi \rangle \xrightarrow{n \to +\infty} \langle T, \varphi \rangle \quad \text{dans } \mathbb{R}.
		\]
		On note alors $T_n \xrightarrow{\mathcal{D}'} T$, et $T$ est appelée la limite de $(T_n)$.
	\end{definition}
	
	\textbf{Nature de cette convergence.} Il s'agit d'une convergence faible : on ne demande pas que les $T_n$ se rapprochent dans un sens géométrique ou en norme. On demande seulement que leur action sur chaque fonction test converge. C'est une condition bien moins exigeante que la convergence uniforme ou $L^p$, ce qui rend $\mathcal{D}'(\Omega)$ très souple.
	
	Si $(T_n)$ est une suite de distributions telle que $\langle T_n, \varphi \rangle$ converge dans $\mathbb{R}$ pour toute $\varphi \in \mathcal{D}(\Omega)$, alors l'application $\varphi \mapsto \lim_n \langle T_n, \varphi \rangle$ est encore une distribution. Autrement dit, $\mathcal{D}'(\Omega)$ est complet pour la convergence faible : on ne peut pas « sortir » de l'espace des distributions en prenant des limites.
	
	Ce théorème est remarquable : dans les espaces de fonctions classiques ($C^0$, $L^p$), une limite de fonctions régulières peut être irrégulière et sortir de l'espace. Ici, toute limite de distributions est encore une distribution — l'espace $\mathcal{D}'(\Omega)$ est stable par passage à la limite.
	
	\section{Le Dirac comme limite de distributions}
	
	On illustre la définition par un exemple purement distributionnel, qui ne fait intervenir aucune intégrale.
	
	\begin{example}[$n(\delta_{1/n} - \delta_{-1/n})$ converge dans $\mathcal{D}'$]
		Pour $n \ge 1$, posons $T_n = n(\delta_{1/n} - \delta_{-1/n})$. Chaque $T_n$ est une combinaison de deux masses de Dirac — c'est une distribution, mais pas une fonction.
		
		Calcul de $\langle T_n, \varphi \rangle$. Pour tout $\varphi \in \mathcal{D}(\mathbb{R})$ :
		\[
		\langle T_n, \varphi \rangle = n(\langle \delta_{1/n}, \varphi \rangle - \langle \delta_{-1/n}, \varphi \rangle) = n\bigl(\varphi(\tfrac{1}{n}) - \varphi(-\tfrac{1}{n})\bigr).
		\]
		
		Passage à la limite. En écrivant le développement limité à l'ordre $1$ de $\varphi$ en $0$ :
		\[
		\varphi(\tfrac{1}{n}) = \varphi(0) + \tfrac{1}{n}\varphi'(0) + o(\tfrac{1}{n}), \quad
		\varphi(-\tfrac{1}{n}) = \varphi(0) - \tfrac{1}{n}\varphi'(0) + o(\tfrac{1}{n}).
		\]
		En soustrayant :
		\[
		\langle T_n, \varphi \rangle = n \cdot \frac{2}{n} \varphi'(0) + o(1) = 2\varphi'(0) + o(1) \xrightarrow{n \to +\infty} 2\varphi'(0).
		\]
		L'application $\varphi \mapsto 2\varphi'(0)$ est bien une forme linéaire continue sur $\mathcal{D}(\mathbb{R})$, donc une distribution. On a ainsi :
		\[
		T_n \xrightarrow{\mathcal{D}'} T \quad \text{où} \quad \langle T, \varphi \rangle = 2\varphi'(0).
		\]
		La limite $T$ définie par $\langle T, \varphi \rangle = 2\varphi'(0)$ n'est associée à aucune fonction : il n'existe pas de $f$ telle que $\int f(x)\varphi(x)\,dx = 2\varphi'(0)$. C'est une distribution singulière, obtenue comme limite de distributions elles-mêmes singulières — ce que l'espace $\mathcal{D}'$ absorbe sans difficulté grâce au théorème de complétude.
		
		\textbf{Remarque.} Pour mémoire, on peut aussi approcher $\delta_0$ par des fonctions régulières : par exemple $f_n(x) = \frac{n}{\sqrt{\pi}} e^{-n^2 x^2}$ converge vers $\delta_0$ dans $\mathcal{D}'(\mathbb{R})$ (changement de variable $t = nx$, puis convergence dominée).
	\end{example}
	
	\begin{figure}[htbp]
		\centering
		\begin{tikzpicture}[scale=1.2,
			axisstyle/.style={-Stealth, semithick, black},
			arrowstyle/.style={-{Stealth[length=5pt, width=4pt]}, thick, black}]
			
			% Axes
			\draw[axisstyle] (-3.5,0) -- (3.5,0) node[right] {$x$};
			\draw[axisstyle] (0,-0.2) -- (0,2.8) node[above] {$y$};
			
			% Gaussiennes normalisées f_n(x) = (n/√π) * exp(-n² x²)
			% n=1
			\draw[domain=-3.2:3.2, smooth, variable=\x, black!30, thick]
			plot ({\x}, { (1/sqrt(pi)) * exp(-\x*\x) });
			% n=2
			\draw[domain=-3.2:3.2, smooth, variable=\x, black!60, thick]
			plot ({\x}, { (2/sqrt(pi)) * exp(-4*\x*\x) });
			% n=4
			\draw[domain=-3.2:3.2, smooth, variable=\x, black, thick]
			plot ({\x}, { (4/sqrt(pi)) * exp(-16*\x*\x) });
			
			% Légende des courbes
			\node[anchor=west, font=\small] at (2.2,2.3) {\textcolor{black!30}{$n=1$}};
			\node[anchor=west, font=\small] at (2.2,1.8) {\textcolor{black!60}{$n=2$}};
			\node[anchor=west, font=\small] at (2.2,1.2) {$n=4$};
			
			% Flèche pour symboliser la masse de Dirac δ₀
			\draw[arrowstyle] (0,0) -- (0,2.4);
			\node[above] at (0,2.4) {$\delta_0$};
			
			% Note : f_n → δ₀ dans D'
			\node[anchor=north west, font=\small\itshape, text=gray] at (-3.3,2.6) 
			{$f_n(x) = \frac{n}{\sqrt{\pi}} e^{-n^2 x^2}$};
		\end{tikzpicture}
		\caption{Approximation de la masse de Dirac $\delta_0$ par des gaussiennes $f_n(x) = \frac{n}{\sqrt{\pi}} e^{-n^2 x^2}$ qui deviennent de plus en plus étroites et hautes.}
		\label{fig:dirac_approx}
	\end{figure}
	
	\section{Stabilité de la limite}
	
	Un résultat fondamental : les opérations classiques sur les distributions sont compatibles avec la convergence dans $\mathcal{D}'$.
	
	Soit $(T_n)$ une suite de distributions convergeant vers $T$ dans $\mathcal{D}'(\Omega)$. Alors pour tout multi-indice $\alpha$ et toute fonction $f \in C^\infty(\Omega)$ :
	\begin{enumerate}
		\item[(i)] Stabilité par dérivation : $\partial^\alpha T_n \to \partial^\alpha T$.
		\item[(ii)] Stabilité par multiplication : $f \cdot T_n \to f \cdot T$.
	\end{enumerate}
	
	\textbf{Démonstration du point (i) en dimension 1.} Pour tout $\varphi \in \mathcal{D}(\mathbb{R})$ :
	\[
	\langle T_n', \varphi \rangle = -\langle T_n, \varphi' \rangle \xrightarrow{n \to \infty} -\langle T, \varphi' \rangle = \langle T', \varphi \rangle.
	\]

	%----------------------------------------------------------
	\begin{qcmblock}
		\hypertarget{qcm:ch2:start}{}\label{qcm:ch2:start}
		\hypertarget{qcm:ch2:q1}{}\label{qcm:ch2:q1}
		\textbf{Question 1.} $(T_n)$ converge vers $T$ dans $\mathcal{D}'(\Omega)$ signifie :
		\begin{enumerate}[label=\Alph*., itemsep=2pt]
			\item $\|T_n - T\| \to 0$ pour une certaine norme sur $\mathcal{D}'$.
			\item Pour toute $\varphi \in \mathcal{D}(\Omega)$, $\langle T_n, \varphi \rangle \to \langle T, \varphi \rangle$ dans $\mathbb{R}$.
			\item $T_n(x) \to T(x)$ pour presque tout $x \in \Omega$.
			\item $T_n$ et $T$ ont le même support pour $n$ assez grand.
		\end{enumerate}
		\navtoQCMRep{ch2}{1}
		
		\medskip
		\hypertarget{qcm:ch2:q2}{}\label{qcm:ch2:q2}
		\textbf{Question 2.} Si $T_n \xrightarrow{\mathcal{D}'} T$, qu'est-ce qu'on peut affirmer ?
		\begin{enumerate}[label=\Alph*., itemsep=2pt]
			\item $T_n' \xrightarrow{\mathcal{D}'} T'$ pour tout ordre de dérivation.
			\item $T_n$ ne converge pas en général.
			\item $T_n$ converge seulement si les $T_n$ sont des fonctions $C^1$.
			\item On ne peut rien dire sans hypothèse supplémentaire.
		\end{enumerate}
		\navtoQCMRep{ch2}{2}
	\end{qcmblock}
	%----------------------------------------------------------
	\begin{exerciceblock}{Convergence vers le Dirac}
		\hypertarget{exo:ch2:ex1}{}\label{exo:ch2:ex1}
		
		Pour $n \ge 1$, on définit $h_n : \mathbb{R} \to \mathbb{R}$ par
		\[
		h_n(x) = \frac{n}{\pi(1+n^2 x^2)}.
		\]
		
		\begin{enumerate}
			\item Vérifier que $\displaystyle \int_{-\infty}^{+\infty} h_n(x)\,dx = 1$ pour tout $n$.
			\item Montrer que $h_n \xrightarrow{\mathcal{D}'} \delta_0$ dans $\mathcal{D}'(\mathbb{R})$.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch2}{1}
	
	%----------------------------------------------------------
	\begin{retenir}{Chapitre 2}
		\begin{itemize}
			\item $T_n \xrightarrow{\mathcal{D}'} T$ signifie : $\langle T_n, \varphi \rangle \to \langle T, \varphi \rangle$ dans $\mathbb{R}$ pour toute fonction test $\varphi$. C'est une convergence faible.
			\item $\mathcal{D}'(\Omega)$ est complet : toute suite « raisonnable » de distributions converge encore vers une distribution.
			\item Toute suite de fonctions positives d'intégrale $1$ dont le support se concentre en $0$ converge vers $\delta_0$ — indépendamment de la forme (gaussienne, créneau, Cauchy...).
			\item La convergence dans $\mathcal{D}'$ est stable par dérivation : $T_n \to T \Rightarrow \partial^\alpha T_n \to \partial^\alpha T$. C'est faux en général pour les fonctions classiques.
		\end{itemize}
	\end{retenir}
	
	% ============================================================
	% CHAPITRE 3 : DÉRIVATION DES DISTRIBUTIONS
	% ============================================================
	\chapter{Dérivation des distributions}
	
	%----------------------------------------------------------
	\section{Motivation — le paradigme de Schwartz}
	%----------------------------------------------------------
	
	Soit $f : \mathbb{R} \to \mathbb{C}$ une fonction de classe $C^1$. Pour toute $\varphi \in \mathcal{D}(\mathbb{R})$, une intégration par parties donne
	\[
	\int_{-\infty}^{+\infty} f'(x)\,\varphi(x)\,dx
	\;=\;
	\Bigl[f(x)\varphi(x)\Bigr]_{-\infty}^{+\infty}
	- \int_{-\infty}^{+\infty} f(x)\,\varphi'(x)\,dx
	\;=\;
	-\int_{-\infty}^{+\infty} f(x)\,\varphi'(x)\,dx,
	\]
	le crochet étant nul car $\varphi$ est à support compact.  Autrement dit,
	\[
	\langle T_{f'}, \varphi \rangle = - \langle T_f, \varphi' \rangle.
	\]
	La distribution associée à la dérivée classique $f'$ n'est autre que l'application
	$\varphi \mapsto -\langle T_f, \varphi'\rangle$.
	\textbf{L'idée fondatrice de Schwartz} : \emph{définir} la dérivée de n'importe quelle distribution $T$ par la même formule, sans jamais supposer que $T$ provient d'une fonction différentiable.
	
	%----------------------------------------------------------
	\section{Définition de la dérivée}
	%----------------------------------------------------------
	
	\begin{definition}[Dérivée d'une distribution]
		Soit $T \in \mathcal{D}'(\mathbb{R})$.
		\begin{enumerate}
			\item La \textbf{dérivée} $T'$ de $T$ est la distribution définie par
			\[
			\langle T', \varphi \rangle = -\langle T, \varphi' \rangle,
			\qquad \forall\,\varphi \in \mathcal{D}(\mathbb{R}).
			\]
			\item Plus généralement, la \textbf{dérivée d'ordre $m$} de $T$ est
			\[
			\langle T^{(m)}, \varphi \rangle = (-1)^m\,\langle T, \varphi^{(m)} \rangle,
			\qquad \forall\,\varphi \in \mathcal{D}(\mathbb{R}),\; m \in \mathbb{N}.
			\]
		\end{enumerate}
	\end{definition}
	
	\begin{remarque}
		La définition est légitime : $\varphi \mapsto -\langle T, \varphi'\rangle$ est bien une forme linéaire continue sur $\mathcal{D}(\mathbb{R})$. En effet, si $\varphi_n \to \varphi$ dans $\mathcal{D}$, alors $\varphi_n' \to \varphi'$ dans $\mathcal{D}$, et la continuité de $T$ donne
		$\langle T, \varphi_n'\rangle \to \langle T, \varphi'\rangle$.
	\end{remarque}
	
	%----------------------------------------------------------
	\section{Toute distribution est indéfiniment dérivable}
	%----------------------------------------------------------
	
	\begin{theorem}[Dérivabilité infinie dans $\mathcal{D}'$]
		Toute distribution $T \in \mathcal{D}'(\mathbb{R})$ admet des dérivées de tous ordres dans $\mathcal{D}'(\mathbb{R})$, et l'opérateur de dérivation $T \mapsto T'$ est un endomorphisme \emph{continu} de $\mathcal{D}'(\mathbb{R})$.
		En d'autres termes : si $(T_n)$ est une suite de distributions convergeant vers $T$ dans $\mathcal{D}'$, alors $T_n^{(k)} \to T^{(k)}$ dans $\mathcal{D}'$ pour tout $k \geq 0$.
	\end{theorem}
	
	\begin{proof}
		\emph{Existence.} L'application $\varphi \mapsto (-1)^k\langle T, \varphi^{(k)}\rangle$ est bien linéaire et continue sur $\mathcal{D}$ : si $\varphi_n \to \varphi$ dans $\mathcal{D}$, les dérivées $\varphi_n^{(k)}$ convergent uniformément vers $\varphi^{(k)}$ (avec supports dans un compact commun), donc $\langle T, \varphi_n^{(k)}\rangle \to \langle T, \varphi^{(k)}\rangle$.
		
		\emph{Continuité de l'opérateur.} Si $T_n \to T$ dans $\mathcal{D}'$, alors pour toute $\varphi \in \mathcal{D}$ :
		\[
		\langle T_n', \varphi \rangle = -\langle T_n, \varphi' \rangle \xrightarrow{n\to\infty} -\langle T, \varphi' \rangle = \langle T', \varphi \rangle. \qedhere
		\]
	\end{proof}
	
	\begin{corollary}
		Toute fonction continue (ou localement intégrable) est \emph{indéfiniment dérivable au sens des distributions}. Sa $k$-ième dérivée distributionnelle peut toutefois être une distribution singulière dès que $k$ dépasse la régularité classique.
	\end{corollary}
	
	%----------------------------------------------------------
	\section{Propriétés algébriques et topologiques}
	%----------------------------------------------------------
	
	\begin{proposition}[Propriétés de la dérivation]
		Soient $T, S \in \mathcal{D}'(\mathbb{R})$, $\alpha, \beta \in \mathbb{C}$, $a \in C^\infty(\mathbb{R})$, $h \in \mathbb{R}$, $\lambda \in \mathbb{R}^*$.
		\begin{enumerate}
			\item \textbf{Linéarité :} $(\alpha T + \beta S)' = \alpha T' + \beta S'$.
			\item \textbf{Continuité séquentielle :} $T_n \xrightarrow{\mathcal{D}'} T \Rightarrow T_n^{(k)} \xrightarrow{\mathcal{D}'} T^{(k)}$ pour tout $k$.
			\item \textbf{Règle de Leibniz :} $(aT)' = a'T + aT'$.
			\item \textbf{Commutation avec les translations :} Si $\tau_h T$ est définie par $\langle \tau_h T, \varphi\rangle = \langle T, \tau_{-h}\varphi\rangle$, alors $(\tau_h T)' = \tau_h(T')$.
			\item \textbf{Dérivée comme limite de différences finies :}
			\[
			T' = \lim_{h\to 0} \frac{T - \tau_h T}{h} \quad \text{dans } \mathcal{D}'(\mathbb{R}).
			\]
			\item \textbf{Homothétie :} Si $\langle T(\lambda x), \varphi\rangle := \frac{1}{|\lambda|}\langle T, \varphi(\cdot/\lambda)\rangle$, alors $\frac{d}{dx}T(\lambda x) = \lambda\,T'(\lambda x)$.
		\end{enumerate}
	\end{proposition}
	
	\begin{proof}
		\emph{Leibniz (point 3).} Pour toute $\varphi \in \mathcal{D}$ :
		\[
		\langle (aT)', \varphi \rangle = -\langle aT, \varphi'\rangle = -\langle T, a\varphi'\rangle
		= -\langle T, (a\varphi)' - a'\varphi\rangle = \langle T', a\varphi\rangle + \langle T, a'\varphi\rangle
		= \langle aT', \varphi\rangle + \langle a'T, \varphi\rangle. \qedhere
		\]
	\end{proof}
	
	%----------------------------------------------------------
	\section{Multiplication d'une distribution par une fonction \texorpdfstring{$C^\infty$}{C-infini}}
	%----------------------------------------------------------
	
	\begin{definition}[Produit $aT$]
		Soient $a \in C^\infty(\mathbb{R})$ et $T \in \mathcal{D}'(\mathbb{R})$. On définit $aT \in \mathcal{D}'(\mathbb{R})$ par
		\[
		\langle aT, \varphi \rangle := \langle T, a\varphi \rangle, \qquad \forall\,\varphi \in \mathcal{D}(\mathbb{R}).
		\]
	\end{definition}
	
	\begin{proposition}[Propriétés du produit avec $\delta$ et ses dérivées]
		Soient $a \in C^\infty(\mathbb{R})$ et $n \in \mathbb{N}$.
		\begin{enumerate}
			\item $a\,\delta_0 = a(0)\,\delta_0$, en particulier $x\,\delta_0 = 0$.
			\item $a\,\delta_0' = a(0)\,\delta_0' - a'(0)\,\delta_0$, en particulier $x\,\delta_0' = -\delta_0$.
			\item \textbf{Formule générale :}
			\[
			a\,\delta_0^{(n)} = \sum_{k=0}^{n} \binom{n}{k} (-1)^k a^{(k)}(0)\,\delta_0^{(n-k)}.
			\]
			\item $x\,\delta_0^{(n)} = -n\,\delta_0^{(n-1)}$, et plus généralement $x^m\,\delta_0^{(n)} = 0$ si $m > n$.
			\item $x^n\,\delta_0^{(n)} = (-1)^n\,n!\,\delta_0$.
		\end{enumerate}
	\end{proposition}
	
	\begin{proof}
		\emph{Point 1.} $\langle a\delta_0, \varphi\rangle = \langle \delta_0, a\varphi\rangle = a(0)\varphi(0) = a(0)\langle\delta_0,\varphi\rangle$.
		
		\emph{Point 2.} $\langle a\delta_0', \varphi\rangle = \langle \delta_0', a\varphi\rangle = -(a\varphi)'(0) = -a'(0)\varphi(0) - a(0)\varphi'(0)$, ce qui donne bien $a(0)\delta_0' - a'(0)\delta_0$.
		
		\emph{Point 3 (formule de Leibniz).} Découle de $\langle a\delta_0^{(n)}, \varphi\rangle = \langle\delta_0^{(n)}, a\varphi\rangle = (-1)^n(a\varphi)^{(n)}(0)$ et de la formule de Leibniz pour $(a\varphi)^{(n)}$.
		
		\emph{Point 4.} Prendre $a(x) = x$ dans (3) : $a(0) = 0$, $a'(0) = 1$, $a^{(k)}(0) = 0$ pour $k \geq 2$, ce qui donne $x\delta_0^{(n)} = -n\delta_0^{(n-1)}$.
	\end{proof}
	
	\begin{exemple}[Calculs pratiques]
		$x^2\delta_0'' = 2\delta_0$, \quad $x^2\delta_0' = 0$, \quad $\sin(x)\,\delta_0 = 0$, \quad $\cos(x)\,\delta_0 = \delta_0$, \quad $e^x\,\delta_0' = \delta_0' - \delta_0$.
	\end{exemple}
	
	%----------------------------------------------------------
	\section{Exemples fondamentaux de dérivation}
	%----------------------------------------------------------
	
	\subsection{Dérivée de la fonction de Heaviside}
	
	\begin{proposition}
		Soit $H$ la fonction de Heaviside : $H(x) = 1$ si $x > 0$, $H(x) = 0$ sinon. Alors
		\[
		H' = \delta_0 \quad \text{dans } \mathcal{D}'(\mathbb{R}).
		\]
	\end{proposition}
	
	\begin{proof}
		Pour $\varphi \in \mathcal{D}(\mathbb{R})$ :
		\[
		\langle H', \varphi \rangle = -\langle H, \varphi'\rangle = -\int_0^{+\infty}\varphi'(x)\,dx = \varphi(0) = \langle\delta_0,\varphi\rangle. \qedhere
		\]
	\end{proof}
	
	\subsection{Dérivée de la valeur absolue}
	
	\begin{proposition}
		$(|x|)' = \operatorname{sgn}(x)$ et $(|x|)'' = 2\delta_0$.
	\end{proposition}
	
	\begin{proof}
		\[
		\langle|x|',\varphi\rangle = -\int_{-\infty}^{+\infty}|x|\varphi'(x)\,dx
		= \int_{-\infty}^0 x\varphi'(x)\,dx - \int_0^{+\infty}x\varphi'(x)\,dx.
		\]
		Deux intégrations par parties donnent chacune $\mp\int\operatorname{sgn}(x)\varphi\,dx$, d'où $(|x|)' = \operatorname{sgn}(x)$.
		Puis $\operatorname{sgn}(x) = 2H(x) - 1$ implique $\operatorname{sgn}'(x) = 2H'(x) = 2\delta_0$.
	\end{proof}
	
	\subsection{Dérivées successives de \texorpdfstring{$|x|$}{|x|}}
	
	\[
	|x|^{(0)} = |x|, \quad
	|x|^{(1)} = \operatorname{sgn}(x), \quad
	|x|^{(2)} = 2\delta_0, \quad
	|x|^{(3)} = 2\delta_0', \quad
	|x|^{(n)} = 2\delta_0^{(n-2)} \text{ pour } n \geq 2.
	\]
	
	\subsection{Dérivées successives de \texorpdfstring{$x^n H(x)$}{xn H(x)}}
	
	\begin{proposition}
		Pour tout $n \in \mathbb{N}$, si on pose $f_n(x) = \dfrac{x^n}{n!}H(x)$, alors
		\[
		f_n' = f_{n-1}, \quad f_0' = H' = \delta_0, \quad f_n^{(k)} = f_{n-k} \text{ pour } k \leq n, \quad f_n^{(n+1)} = \delta_0.
		\]
	\end{proposition}
	
	\begin{proof}
		\[
		\left\langle \left(\tfrac{x^n}{n!}H\right)', \varphi\right\rangle
		= -\int_0^{+\infty}\tfrac{x^n}{n!}\varphi'(x)\,dx
		= \int_0^{+\infty}\tfrac{x^{n-1}}{(n-1)!}\varphi(x)\,dx, \quad \text{par IPP (crochet nul)}.
		\]
	\end{proof}
	
	\subsection{Dérivée de \texorpdfstring{$\ln|x|$}{ln|x|}}
	
	\begin{proposition}
		\[
		\frac{d}{dx}\ln|x| = \mathrm{vp}\frac{1}{x} \quad \text{dans } \mathcal{D}'(\mathbb{R}),
		\]
		où $\mathrm{vp}\frac{1}{x}$ désigne la valeur principale de Cauchy définie par
		$\langle \mathrm{vp}\frac{1}{x}, \varphi\rangle = \lim_{\varepsilon\to 0^+}\int_{|x|\geq\varepsilon}\frac{\varphi(x)}{x}\,dx$.
	\end{proposition}
	
	\begin{proof}
		\begin{align*}
			\langle T', \varphi\rangle
			&= -\int_{-\infty}^{+\infty}\ln|x|\,\varphi'(x)\,dx \\
			&= -\int_0^{+\infty}\ln(x)\,\varphi'(x)\,dx - \int_{-\infty}^0\ln(-x)\,\varphi'(x)\,dx.
		\end{align*}
		Pour le premier terme, une intégration par parties avec régularisation en $\varepsilon \to 0^+$ donne $\lim_{\varepsilon\to 0^+}(\ln\varepsilon)\varphi(\varepsilon) + \int_\varepsilon^{+\infty}\frac{\varphi(x)}{x}\,dx$. De même pour le second terme avec $-\ln\varepsilon$. Les logarithmes se compensent, et la somme converge vers $\langle\mathrm{vp}\frac{1}{x}, \varphi\rangle$.
	\end{proof}
	
	\subsection{Dérivée de la partie entière \texorpdfstring{$\lfloor x\rfloor$}{[x]}}
	
	\begin{proposition}
		La fonction partie entière $f(x) = \lfloor x\rfloor$ a pour dérivée le \textbf{peigne de Dirac} :
		\[
		\lfloor x\rfloor' = \sum_{n=-\infty}^{+\infty} \delta_n = \Delta_1.
		\]
	\end{proposition}
	
	\begin{proof}
		$\langle f', \varphi\rangle = -\langle f, \varphi'\rangle = -\displaystyle\sum_{n=-\infty}^{+\infty} n\int_n^{n+1}\varphi'(x)\,dx = \displaystyle\sum_{n=-\infty}^{+\infty} n\bigl[\varphi(n)-\varphi(n+1)\bigr] = \displaystyle\sum_{n=-\infty}^{+\infty}\varphi(n)$ (somme télescopique à support fini).
	\end{proof}
	
	\begin{figure}[htbp]
		\centering
		\begin{tikzpicture}[scale=1.0,
			arrowstyle/.style={-{Stealth[length=5pt, width=4pt]}, thick, black},
			axisstyle/.style={-Stealth, semithick, black},
			labelstyle/.style={font=\small}]
			
			% Axes
			\draw[axisstyle] (-3.8,0) -- (3.8,0) node[right] {$x$};
			\draw[axisstyle] (0,-0.2) -- (0,2.2) node[above] {$y$};
			
			% Flèches représentant les masses de Dirac
			\foreach \n in {-3,...,3} {
				\draw[arrowstyle] (\n,0) -- (\n,1.8);
				\node[labelstyle] at (\n,-0.25) {$\n$};
			}
			
			% Légende
			\node[anchor=west, font=\small\itshape] at (1.2,1.9) {$\Delta_1 = \displaystyle\sum_{n\in\mathbb{Z}} \delta_n$};
			
			% Ligne horizontale pointillée pour guider l'œil (optionnel)
			\draw[gray, densely dotted, thin] (-3.5,0) -- (3.5,0);
		\end{tikzpicture}
		\caption{Représentation du peigne de Dirac $\Delta_1 = \sum_{n\in\mathbb{Z}} \delta_n$.}
		\label{fig:peigne_dirac}
	\end{figure}
	%----------------------------------------------------------
	\section{Formule des sauts — fonctions \texorpdfstring{$C^\infty$}{C-infini} par morceaux}
	%----------------------------------------------------------
	
	\begin{theorem}[Formule des sauts — cas général]
		Soit $f : \mathbb{R} \to \mathbb{C}$ une fonction $C^\infty$ sur $\mathbb{R}$ sauf en un nombre localement fini de points $(x_\nu)_{\nu\in\mathbb{Z}}$ avec $x_\nu \to \pm\infty$. On note, pour $p \geq 0$ :
		\[
		[f^{(p)}]_\nu := f^{(p)}(x_\nu^+) - f^{(p)}(x_\nu^-) \quad \text{(saut de la dérivée d'ordre $p$ en $x_\nu$)},
		\]
		et $[f^{(p)}]$ la fonction qui vaut $f^{(p)}$ entre les points de discontinuité (non définie en ceux-ci). Alors, au sens des distributions :
		\[
		f' = [f'] + \sum_\nu [f]_\nu\,\delta_{x_\nu},
		\]
		\[
		f'' = [f''] + \sum_\nu [f']_\nu\,\delta_{x_\nu} + \sum_\nu [f]_\nu\,\delta_{x_\nu}',
		\]
		et plus généralement :
		\[
		f^{(n)} = [f^{(n)}] + \sum_{k=0}^{n-1}\sum_\nu [f^{(k)}]_\nu\,\delta_{x_\nu}^{(n-1-k)}.
		\]
	\end{theorem}
	
	\begin{proof}
		Une intégration par parties sur chaque intervalle $(x_\nu, x_{\nu+1})$ donne, après sommation :
		\[
		\langle f', \varphi\rangle = -\langle f, \varphi'\rangle = \int_{-\infty}^{+\infty}[f'](x)\varphi(x)\,dx + \sum_\nu [f]_\nu\,\varphi(x_\nu).
		\]
		On applique la formule récursivement pour les ordres supérieurs.
	\end{proof}
	
	\begin{exemple}[Fonction créneau]
		Soit $f = \mathbf{1}_{[a,b]}$ (indicatrice de $[a,b]$). Alors $f$ a pour sauts $[f]_a = 1$ et $[f]_b = -1$. Donc
		\[
		f' = \delta_a - \delta_b, \qquad f'' = \delta_a' - \delta_b'.
		\]
	\end{exemple}
	
	\begin{exemple}[Fonction triangulaire]
		Soit $f(x) = \max(1 - |x|, 0)$ (chapeau sur $[-1,1]$). Elle est $C^0$ mais pas $C^1$. On a $[f']_{-1} = 1$, $[f']_0 = -2$, $[f']_1 = 1$ ; les sauts de $f$ en ces points sont nuls. Donc
		\[
		f'' = [f''] + \delta_{-1} - 2\delta_0 + \delta_1 = -2\delta_0 + \delta_{-1} + \delta_1.
		\]
		(car $[f''] = 0$ en dehors des points de non-dérivabilité, et $f$ est nulle hors de $[-1,1]$).
	\end{exemple}
	
	\begin{figure}[htbp]
		\centering
		\begin{tikzpicture}[scale=1.1,
			arrowstyle/.style={-{Stealth[length=5pt, width=4pt]}, ultra thick, black},
			axisstyle/.style={-Stealth, semithick, gray!70}, 
			functionstyle/.style={ultra thick, black},
			labelstyle/.style={font=\scriptsize}] % Texte plus petit pour gagner de la place
			
			% --- GRAPH 1 : f(x) ---
			\begin{scope}[shift={(0,0)}]
				\draw[axisstyle] (-1.5,0) -- (1.5,0) node[right, black] {$x$};
				\draw[axisstyle] (0,-0.2) -- (0,1.3) node[above, black] {$f$};
				\draw[functionstyle] (-1.4,0.03) -- (-1,0.03) -- (0,1) -- (1,0.03) -- (1.4,0.03);
				\node[below, labelstyle] at (-1,0) {$-1$};
				\node[below, labelstyle] at (1,0) {$1$};
				\node[below, labelstyle] at (0,-0.5) {\textbf{Fonction $f$}};
			\end{scope}
			
			% --- GRAPH 2 : f'(x) ---
			\begin{scope}[shift={(3.8,0)}]
				\draw[axisstyle] (-1.5,0) -- (1.5,0) node[right, black] {$x$};
				\draw[axisstyle] (0,-1.3) -- (0,1.3) node[above, black] {$f'$};
				\draw[functionstyle] (-1.4,0.03) -- (-1,0.03); 
				\draw[functionstyle] (-1,1) -- (0,1);
				\draw[functionstyle] (0,-1) -- (1,-1);
				\draw[functionstyle] (1,0.03) -- (1.4,0.03);
				\draw[black, dashed, thin] (-1,0) -- (-1,1);
				\draw[black, dashed, thin] (0,1) -- (0,-1);
				\draw[black, dashed, thin] (1,-1) -- (1,0);
				\node[below, labelstyle] at (0,-1.4) {\textbf{Dérivée $f'$}};
			\end{scope}
			
			% --- GRAPH 3 : f''(x) ---
			\begin{scope}[shift={(7.6,0)}]
				\draw[axisstyle] (-1.5,0) -- (1.5,0) node[right, black] {$x$};
				\draw[axisstyle] (0,-1.3) -- (0,1.3) node[above, black] {$f''$};
				\draw[arrowstyle] (-1,0) -- (-1,0.8) node[above, labelstyle] {$\delta_{-1}$};
				\draw[arrowstyle] (0,0) -- (0,-1.1) node[right, labelstyle] {$-2\delta_0$};
				\draw[arrowstyle] (1,0) -- (1,0.8) node[above, labelstyle] {$\delta_{1}$};
				\node[below, labelstyle] at (0,-1.4) {\textbf{Dérivée $f''$}};
			\end{scope}
			
		\end{tikzpicture}
		\caption{Succession des dérivées de la fonction triangle au sens des distributions.}
	\end{figure}
	
	
	%----------------------------------------------------------
	\section{Dérivée de la distribution de Dirac et de ses itérées}
	%----------------------------------------------------------
	
	\begin{proposition}
		Pour tout $m \in \mathbb{N}$ et $a \in \mathbb{R}$ :
		\[
		\langle \delta_a^{(m)}, \varphi \rangle = (-1)^m\,\varphi^{(m)}(a).
		\]
		En particulier $\delta_a^{(m)}$ est à support $\{a\}$ et est une distribution singulière pour tout $m \geq 1$.
	\end{proposition}
	
	\begin{theorem}[Structure des distributions à support ponctuel]
		Soit $T \in \mathcal{D}'(\mathbb{R})$ à support $\{0\}$. Alors il existe $N \in \mathbb{N}$ et des constantes $c_0, \dots, c_N \in \mathbb{C}$ tels que
		\[
		T = \sum_{k=0}^N c_k\,\delta_0^{(k)}.
		\]
	\end{theorem}
	
	\begin{remarque}
		Ce résultat est très important : toute distribution dont le support est réduit à un point est \emph{nécessairement} une combinaison linéaire finie de la mesure de Dirac et de ses dérivées.
	\end{remarque}
	
	%----------------------------------------------------------
	\section{Formule des distributions \texorpdfstring{$\partial_j T$}{} en dimension \texorpdfstring{$n$}{n}}
	%----------------------------------------------------------
	
	En dimension $n \geq 1$, si $T \in \mathcal{D}'(\mathbb{R}^n)$ et $\alpha \in \mathbb{N}^n$, on définit
	\[
	\langle \partial^\alpha T, \varphi \rangle = (-1)^{|\alpha|}\langle T, \partial^\alpha \varphi\rangle,
	\qquad \forall\,\varphi \in \mathcal{D}(\mathbb{R}^n).
	\]
	Toutes les propriétés précédentes s'étendent \emph{sans modification}.
	
	
	%----------------------------------------------------------
	\section{QCM — Dérivation des distributions}
	%----------------------------------------------------------
	
	\begin{qcmblock}
		\hypertarget{qcm:ch3:start}{}\label{qcm:ch3:start}
		\hypertarget{qcm:ch3:q1}{}\label{qcm:ch3:q1}
		\textbf{Question 1.} Quelle est la dérivée de $f(x) = x_+ := \max(x,0)$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $H(x)$\quad B. $\delta_0$\quad C. $H(-x)$\quad D. $|x|$\\
		\navtoQCMRep{ch3}{1}
		
		\medskip
		\hypertarget{qcm:ch3:q2}{}\label{qcm:ch3:q2}
		\textbf{Question 2.} Quelle est la valeur de $\langle \delta_0^{(3)}, \varphi \rangle$ ?\\
		A. $\varphi^{(3)}(0)$\quad B. $-\varphi^{(3)}(0)$\quad C. $3\varphi(0)$\quad D. $\varphi''(0)$\\
		\navtoQCMRep{ch3}{2}
		
		\medskip
		\hypertarget{qcm:ch3:q3}{}\label{qcm:ch3:q3}
		\textbf{Question 3.} Soit $f(x) = |x|^3$. Quelle est $f''$ dans $\mathcal{D}'$ ?\\
		A. $6|x|$\quad B. $6x\,\mathrm{sgn}(x)$\quad C. $6|x| + \delta_0$\quad D. $3x^2\,\mathrm{sgn}(x)$\\
		\navtoQCMRep{ch3}{3}
		
		\medskip
		\hypertarget{qcm:ch3:q4}{}\label{qcm:ch3:q4}
		\textbf{Question 4.} Quel est $x\,\delta_0''$ ?\\
		A. $0$\quad B. $-2\delta_0$\quad C. $-\delta_0'$\quad D. $2\delta_0$\\
		\navtoQCMRep{ch3}{4}
		
		\medskip
		\hypertarget{qcm:ch3:q5}{}\label{qcm:ch3:q5}
		\textbf{Question 5.} Soit $f(x) = \mathbf{1}_{[0,1]}(x)$. Quelle est $f'$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $0$\quad B. $\delta_0 + \delta_1$\quad C. $\delta_0 - \delta_1$\quad D. $-\delta_0 + \delta_1$\\
		\navtoQCMRep{ch3}{5}
		
		\medskip
		\hypertarget{qcm:ch3:q6}{}\label{qcm:ch3:q6}
		\textbf{Question 6.} La dérivée distributionnelle de $e^{|x|}$ est :\\
		A. $e^{|x|}\mathrm{sgn}(x)$\quad B. $e^{|x|}\mathrm{sgn}(x) + 2\delta_0$\quad C. $|x|e^{|x|}$\quad D. $e^x H(x) - e^{-x}H(-x)$\\
		\navtoQCMRep{ch3}{6}
		
		\medskip
		\hypertarget{qcm:ch3:q7}{}\label{qcm:ch3:q7}
		\textbf{Question 7.} Si $T = \displaystyle\sum_{n \in \mathbb{Z}} \delta_n$ (peigne de Dirac), quelle est $T'$ ?\\
		A. $\displaystyle\sum_{n\in\mathbb{Z}}\delta_n'$\quad B. $0$\quad C. $T$\quad D. $\displaystyle\sum_{n\in\mathbb{Z}} n\,\delta_n$\\
		\navtoQCMRep{ch3}{7}
		
		\medskip
		\hypertarget{qcm:ch3:q8}{}\label{qcm:ch3:q8}
		\textbf{Question 8.} Parmi les énoncés suivants, lequel est \emph{faux} ?\\
		A. $\delta_0' \in \mathcal{D}'(\mathbb{R})$.\\
		B. $H \in \mathcal{D}'(\mathbb{R})$ et $H' = \delta_0$.\\
		C. Toute distribution est indéfiniment dérivable.\\
		D. Si $f \in C^0(\mathbb{R})$ et $f$ n'est pas $C^1$, alors $f'$ n'existe pas dans $\mathcal{D}'(\mathbb{R})$.\\
		\navtoQCMRep{ch3}{8}
		
		\medskip
		\hypertarget{qcm:ch3:q9}{}\label{qcm:ch3:q9}
		\textbf{Question 9.} Quelle est la dérivée de $\mathrm{vp}\dfrac{1}{x}$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $-\mathrm{Pf}\dfrac{1}{x^2}$\quad B. $0$\quad C. $\dfrac{1}{x^2}$\quad D. $\mathrm{vp}\dfrac{1}{x^2}$\\
		\navtoQCMRep{ch3}{9}
		
		\medskip
		\hypertarget{qcm:ch3:q10}{}\label{qcm:ch3:q10}
		\textbf{Question 10.} Soit $T \in \mathcal{D}'(\mathbb{R})$ à support $\{0\}$. Alors $T$ est nécessairement :\\
		A. proportionnelle à $\delta_0$.\\
		B. une combinaison finie de $\delta_0, \delta_0', \delta_0'', \ldots$\\
		C. nulle.\\
		D. une mesure bornée.\\
		\navtoQCMRep{ch3}{10}
	\end{qcmblock}
	%----------------------------------------------------------
	\section{Exercices — Dérivation}
	%----------------------------------------------------------
	
	\begin{exerciceblock}{Dérivation de fonctions par morceaux}
		\hypertarget{exo:ch3:ex1}{}\label{exo:ch3:ex1}
		Calculer les dérivées $f'$, $f''$ et $f'''$ dans $\mathcal{D}'(\mathbb{R})$ de la fonction définie par
		\[
		f(x) = \begin{cases} 0 & \text{si } x < 0, \\ x^2 & \text{si } 0 \leq x \leq 1, \\ 2-x & \text{si } x > 1. \end{cases}
		\]
	\end{exerciceblock}
	\navtoCorr{ch3}{1}
	
	\begin{exerciceblock}{Action de $x\,\delta_0^{(n)}$}
		\hypertarget{exo:ch3:ex2}{}\label{exo:ch3:ex2}
		Montrer que $x^2\,\delta_0'' = 2\delta_0$ et $x^3\,\delta_0'' = 0$.
	\end{exerciceblock}
	\navtoCorr{ch3}{2}
	
	\begin{exerciceblock}{Dérivée de $|x|\sin x$}
		\hypertarget{exo:ch3:ex3}{}\label{exo:ch3:ex3}
		Calculer les dérivées d'ordre $1$ et $2$ de $T = |x|\sin x$ dans $\mathcal{D}'(\mathbb{R})$.
	\end{exerciceblock}
	\navtoCorr{ch3}{3}
	
	\begin{exerciceblock}{Vérification par deux méthodes}
		\hypertarget{exo:ch3:ex4}{}\label{exo:ch3:ex4}
		Montrer que $\dfrac{d}{dx}(H(x)\sin x) = \delta_0 + H(x)\cos x$.
	\end{exerciceblock}
	\navtoCorr{ch3}{4}
	
	\begin{exerciceblock}{Dérivée du peigne de Dirac et série trigonométrique}
		\hypertarget{exo:ch3:ex5}{}\label{exo:ch3:ex5}
		Montrer que $\dfrac{d}{dx}\Delta_a = \displaystyle\sum_{n\in\mathbb{Z}}\delta_{na}'$, et en déduire que, au sens des distributions,
		\[
		\sum_{n=1}^{+\infty} n\sin(nx) = -\pi\sum_{k\in\mathbb{Z}} \delta_{2k\pi}'.
		\]
	\end{exerciceblock}
	\navtoCorr{ch3}{5}
	
	% ============================================================
	% CHAPITRE 4 : PRIMITIVES DES DISTRIBUTIONS
	% ============================================================
	\chapter{Primitives des distributions en dimension 1}
	
	%----------------------------------------------------------
	\section{Définition et premières remarques}
	%----------------------------------------------------------
	
	\begin{definition}[Primitive d'une distribution]
		Soit $S \in \mathcal{D}'(\mathbb{R})$. On appelle \textbf{primitive} de $S$ toute distribution $T \in \mathcal{D}'(\mathbb{R})$ vérifiant
		\[
		T' = S.
		\]
	\end{definition}
	
	\begin{remarque}
		Cette définition est la transposée exacte de la définition classique. Si $f \in C^0(\mathbb{R})$, ses primitives au sens classique et ses primitives au sens des distributions coïncident (à constante près).
	\end{remarque}
	
	%----------------------------------------------------------
	\section{Le noyau de la dérivation : distributions de dérivée nulle}
	%----------------------------------------------------------
	
	\begin{lemma}[Caractérisation des distributions de dérivée nulle]
		Soit $T \in \mathcal{D}'(\mathbb{R})$. Alors
		\[
		T' = 0 \quad \text{dans } \mathcal{D}'(\mathbb{R})
		\iff
		\exists\, C \in \mathbb{C}, \quad T = C
		\quad\left(\text{i.e. } \langle T, \varphi\rangle = C\int_{-\infty}^{+\infty}\varphi(x)\,dx\right).
		\]
	\end{lemma}
	
	\begin{proof}
		\emph{($\Leftarrow$)} Si $T = C$ (constante), alors $\langle T', \varphi\rangle = -\langle T, \varphi'\rangle = -C\int\varphi' = 0$.
		
		\emph{($\Rightarrow$)} Supposons $T' = 0$, c'est-à-dire $\langle T, \varphi'\rangle = 0$ pour toute $\varphi \in \mathcal{D}$.
		
		\textbf{Étape 1 (image de la dérivation).} Montrons que l'image de $\varphi \mapsto \varphi'$ dans $\mathcal{D}$ est précisément
		\[
		\mathcal{D}_0 := \left\{\psi \in \mathcal{D}\;\middle|\; \int_{-\infty}^{+\infty}\psi(x)\,dx = 0\right\}.
		\]
		Si $\varphi \in \mathcal{D}$, alors $\varphi' \in \mathcal{D}_0$ (intégrale d'une dérivée à support compact). Réciproquement, si $\psi \in \mathcal{D}_0$, posons $\varphi(x) = \int_{-\infty}^x\psi(t)\,dt$ : c'est $C^\infty$, nul pour $x \ll 0$, et nul pour $x \gg 0$ (car $\int\psi = 0$). Donc $\varphi \in \mathcal{D}$ et $\varphi' = \psi$.
		
		\textbf{Étape 2.} $T' = 0$ signifie que $T$ s'annule sur $\mathcal{D}_0$, qui est un hyperplan de $\mathcal{D}$.
		Fixons $\theta \in \mathcal{D}$ avec $\int\theta = 1$. Toute $\varphi \in \mathcal{D}$ s'écrit
		\[
		\varphi = \underbrace{\left(\varphi - \left(\int\varphi\right)\theta\right)}_{\in\,\mathcal{D}_0} + \left(\int\varphi\right)\theta.
		\]
		Donc $\langle T, \varphi\rangle = \langle T, (\int\varphi)\theta\rangle = \langle T, \theta\rangle \cdot \int\varphi$. Posant $C = \langle T, \theta\rangle$, on obtient $T = C$.
	\end{proof}
	
	%----------------------------------------------------------
	\section{Existence et unicité d'une primitive}
	%----------------------------------------------------------
	
	\begin{theorem}[Existence et unicité à une constante près]
		Toute distribution $S \in \mathcal{D}'(\mathbb{R})$ admet au moins une primitive $T$. L'ensemble des primitives de $S$ est
		\[
		\{T + C \mid C \in \mathbb{C}\},
		\]
		où $T$ est une primitive particulière quelconque. C'est un espace affine de dimension $1$ sur $\mathbb{C}$.
	\end{theorem}
	
	\begin{proof}
		\emph{Unicité modulo constante.} Si $T_1' = T_2' = S$, alors $(T_1 - T_2)' = 0$, donc $T_1 - T_2 = C$ par le lemme précédent.
		
		\emph{Existence.} On construit explicitement une primitive. Reprenons les notations du lemme : fixons $\theta \in \mathcal{D}$ avec $\int\theta = 1$.
		Pour toute $\varphi \in \mathcal{D}$, posons $\Psi_\varphi(x) = \int_{-\infty}^x\bigl(\varphi(t) - (\int\varphi)\theta(t)\bigr)\,dt \in \mathcal{D}$.
		Définissons $T$ par
		\[
		\langle T, \varphi\rangle := -\langle S, \Psi_\varphi\rangle.
		\]
		$T$ est linéaire et continue (car $\varphi \mapsto \Psi_\varphi$ est continue dans $\mathcal{D}$). Vérifions $T' = S$ :
		\[
		\langle T', \varphi\rangle = -\langle T, \varphi'\rangle = \langle S, \Psi_{\varphi'}\rangle.
		\]
		Or $\Psi_{\varphi'} = \varphi$ (car $\int\varphi' = 0$, donc $\Psi_{\varphi'}(x) = \int_{-\infty}^x\varphi'(t)\,dt = \varphi(x)$). Donc $\langle T', \varphi\rangle = \langle S, \varphi\rangle$.
	\end{proof}
	
	%----------------------------------------------------------
	\section{Primitives des distributions usuelles}
	%----------------------------------------------------------
	
	\begin{theorem}[Tableau des primitives fondamentales]
		\mbox{}
		
		\begin{center}
			\renewcommand{\arraystretch}{1.6}
			\begin{tabular}{@{}lll@{}}
				\toprule
				\textbf{Distribution $S$} & \textbf{Primitive particulière $T$} & \textbf{Commentaire} \\
				\midrule
				Constante $C_0$ & $C_0\,x$ & primitives classiques \\
				$H(x)$ & $x_+ = x H(x)$ & $\int_0^x H(t)\,dt = x$ si $x > 0$ \\
				$\delta_0$ & $H(x)$ & $H' = \delta_0$ \\
				$\delta_0'$ & $\delta_0$ & $\delta_0' = (\delta_0)'$ \\
				$\delta_0^{(n)}$ & $\delta_0^{(n-1)}$ & récurrence \\
				$\delta_a$ & $H(x-a) = \tau_a H$ & translation \\
				$\mathrm{sgn}(x)$ & $|x|$ & car $|x|' = \mathrm{sgn}(x)$ \\
				$\mathrm{vp}\dfrac{1}{x}$ & $\ln|x|$ & car $(\ln|x|)' = \mathrm{vp}\dfrac{1}{x}$ \\
				$-\mathrm{Pf}\dfrac{1}{x^2}$ & $\mathrm{vp}\dfrac{1}{x}$ & par définition \\
				$\Delta_a = \displaystyle\sum_{n\in\mathbb{Z}}\delta_{na}$ & $\displaystyle\sum_{n\in\mathbb{Z}}H(x-na)$ & peigne de Heaviside \\
				\bottomrule
			\end{tabular}
		\end{center}
		\renewcommand{\arraystretch}{1}
	\end{theorem}
	
	%----------------------------------------------------------
	\section{Primitive d'ordre \texorpdfstring{$n$}{n} de \texorpdfstring{$\delta_0$}{delta}}
	%----------------------------------------------------------
	
	\begin{proposition}
		La suite de fonctions $\displaystyle f_n(x) = \frac{x^n}{n!}H(x)$ vérifie
		\[
		f_n^{(n+1)} = \delta_0, \qquad f_0 = H, \qquad f_n' = f_{n-1}.
		\]
		En particulier, la primitive d'ordre $n$ de $\delta_0$ qui s'annule sur $]-\infty,0[$ est $f_n(x) = \dfrac{x^n}{n!}H(x)$.
	\end{proposition}
	
	\begin{proof}
		Par récurrence. $f_0' = H' = \delta_0$. Si $f_{n-1}' = f_{n-2}$, alors
		$\left(\frac{x^n}{n!}H\right)' = \frac{x^{n-1}}{(n-1)!}H + \frac{x^n}{n!}\delta_0 = \frac{x^{n-1}}{(n-1)!}H = f_{n-1}$
		(car $x^n\delta_0 = 0$).
	\end{proof}
	
	%----------------------------------------------------------
	\section{Structure des primitives dans \texorpdfstring{$\mathcal{D}'(\mathbb{R})$}{D'(R)}}
	%----------------------------------------------------------
	
	\begin{proposition}[Primitives de distributions à support borné]
		Soit $S \in \mathcal{D}'(\mathbb{R})$ à support compact. Alors toutes ses primitives sont à support borné à gauche (bornées inférieurement), mais pas nécessairement compactes.
	\end{proposition}
	
	\begin{proposition}[Commutation avec la multiplication par $C^\infty$]
		Si $T$ est une primitive de $S$ et $a \in C^\infty(\mathbb{R})$, alors la primitive de $aS$ \emph{n'est pas} $aT$ en général : $(aT)' = a'T + aS$.
	\end{proposition}
	
	\begin{remarque}
		C'est précisément pourquoi la résolution de $T' + a(x)T = S$ dans $\mathcal{D}'$ ne se réduit pas à la recherche d'une primitive, mais demande l'utilisation d'un facteur intégrant $e^{A(x)}$ (voir chapitre~5).
	\end{remarque}
	
	%----------------------------------------------------------
	\section{Lien avec les intégrales à paramètre}
	%----------------------------------------------------------
	
	\begin{theorem}[Primitive via intégrale à paramètre]
		Si $f \in L^1_{\text{loc}}(\mathbb{R})$, la fonction
		\[
		F(x) = \int_a^x f(t)\,dt
		\]
		est continue, localement intégrable, et vérifie $F' = T_f$ dans $\mathcal{D}'(\mathbb{R})$. C'est une primitive classique de $T_f$.
	\end{theorem}
	
	\begin{exemple}
		\begin{itemize}
			\item Primitive de $T_f$ avec $f(x) = e^{-|x|}$ : $F(x) = \int_0^x e^{-|t|}dt$, qui vaut $1 - e^{-x}$ pour $x \geq 0$ et $e^x - 1$ pour $x < 0$.
			\item Primitive de $\mathrm{vp}\frac{1}{x}$ : $\ln|x|$ (déjà vue).
			\item Primitive d'ordre 2 de $\delta_0$ : $\frac{x^2}{2}H(x) = x_+^2/2$.
		\end{itemize}
	\end{exemple}
	
	%----------------------------------------------------------
	\section{Primitives et équation \texorpdfstring{$T' = S$}{T'=S}}
	%----------------------------------------------------------
	
	\begin{theorem}[Résolution de $T' = S$]
		Pour tout $S \in \mathcal{D}'(\mathbb{R})$, la solution générale de l'équation $T' = S$ dans $\mathcal{D}'(\mathbb{R})$ est
		\[
		T = T_0 + C, \quad C \in \mathbb{C},
		\]
		où $T_0$ est une primitive particulière de $S$.
		
		Lorsque $S$ est une fonction localement intégrable, $T_0(x) = \int_0^x S(t)\,dt$ est une primitive particulière.
	\end{theorem}
	
	\begin{exemple}[Primitive de $|x|$]
		$|x|' = \mathrm{sgn}(x)$, donc une primitive de $\mathrm{sgn}(x)$ est $|x|$.
		Vérification : $\langle \mathrm{sgn}', \varphi\rangle = -\int_{-\infty}^{+\infty}\mathrm{sgn}(x)\varphi'(x)\,dx = \int_{-\infty}^0\varphi'(x)\,dx - \int_0^{+\infty}\varphi'(x)\,dx = 2\varphi(0)$.
		Donc $\mathrm{sgn}'= 2\delta_0$ et une primitive de $\mathrm{sgn}$ est bien $|x|/1 = |x|$ (modulo constante).
	\end{exemple}
	
	%----------------------------------------------------------
	\section{QCM — Primitives des distributions}
	%----------------------------------------------------------
	
	\begin{qcmblock}
		\hypertarget{qcm:ch4:start}{}\label{qcm:ch4:start}
		\hypertarget{qcm:ch4:q1}{}\label{qcm:ch4:q1}
		\textbf{Question 1.} Quelle est la primitive de $\delta_0$ s'annulant sur $]-\infty, 0[$ ?\\
		A. $\delta_0'$\quad B. $H(x)$\quad C. $H(x) + 1$\quad D. $x\,\delta_0$\\
		\navtoQCMRep{ch4}{1}
		
		\medskip
		\hypertarget{qcm:ch4:q2}{}\label{qcm:ch4:q2}
		\textbf{Question 2.} Quelle est la solution générale de $T' = 0$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $T = 0$\quad B. $T = \delta_0$\quad C. $T = C$ (constante)\quad D. $T = C\,x$\\
		\navtoQCMRep{ch4}{2}
		
		\medskip
		\hypertarget{qcm:ch4:q3}{}\label{qcm:ch4:q3}
		\textbf{Question 3.} Si $T'' = 0$ dans $\mathcal{D}'(\mathbb{R})$, quelle est la forme générale de $T$ ?\\
		A. $T = C$\quad B. $T = Cx + D$\quad C. $T = C\,\delta_0$\quad D. $T = H$\\
		\navtoQCMRep{ch4}{3}
		
		\medskip
		\hypertarget{qcm:ch4:q4}{}\label{qcm:ch4:q4}
		\textbf{Question 4.} Parmi les distributions suivantes, laquelle est une primitive de $2\delta_0$ ?\\
		A. $2H(x)$\quad B. $\delta_0'$\quad C. $2\delta_0'$\quad D. $H(-x)$\\
		\navtoQCMRep{ch4}{4}
		
		\medskip
		\hypertarget{qcm:ch4:q5}{}\label{qcm:ch4:q5}
		\textbf{Question 5.} La primitive de $\delta_0^{(n)}$ la plus naturelle est :\\
		A. $\delta_0^{(n+1)}$\quad B. $\delta_0^{(n-1)}$\quad C. $\frac{x^n}{n!}H$\quad D. $n!\,\delta_0$\\
		\navtoQCMRep{ch4}{5}
		
		\medskip
		\hypertarget{qcm:ch4:q6}{}\label{qcm:ch4:q6}
		\textbf{Question 6.} Combien y a-t-il de primitives de $\delta_0 + \delta_1$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. Aucune\quad B. Exactement une\quad C. Une infinité\quad D. Deux\\
		\navtoQCMRep{ch4}{6}
		
		\medskip
		\hypertarget{qcm:ch4:q7}{}\label{qcm:ch4:q7}
		\textbf{Question 7.} Quelle est la primitive de $\mathrm{sgn}(x)$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $2\delta_0$\quad B. $|x| + C$\quad C. $H(x) + C$\quad D. $x\,H(x) + C$\\
		\navtoQCMRep{ch4}{7}
		
		\medskip
		\hypertarget{qcm:ch4:q8}{}\label{qcm:ch4:q8}
		\textbf{Question 8.} Soit $f(x) = x^2 H(x)$. Quelle est sa dérivée seconde dans $\mathcal{D}'$ ?\\
		A. $2H(x)$\quad B. $2H(x) + \delta_0$\quad C. $2H(x) + 2\delta_0$\quad D. $2x\,\delta_0 + 2H$\\
		\navtoQCMRep{ch4}{8}
		
		\medskip
		\hypertarget{qcm:ch4:q9}{}\label{qcm:ch4:q9}
		\textbf{Question 9.} La distribution $\ln|x|$ est une primitive de :\\
		A. $\dfrac{1}{x^2}$\quad B. $\mathrm{Pf}\dfrac{1}{x^2}$\quad C. $\mathrm{vp}\dfrac{1}{x}$\quad D. $0$\\
		\navtoQCMRep{ch4}{9}
		
		\medskip
		\hypertarget{qcm:ch4:q10}{}\label{qcm:ch4:q10}
		\textbf{Question 10.} Dans $\mathcal{D}'(\mathbb{R})$, l'équation $T'' = \delta_0$ a pour solution générale :\\
		A. $T = H(x) + C$\quad B. $T = xH(x) + Cx + D$\quad C. $T = \frac{x^2}{2}H(x) + Cx + D$\quad D. $T = \delta_0' + Cx + D$\\
		\navtoQCMRep{ch4}{10}
	\end{qcmblock}
	%----------------------------------------------------------
	\section{Exercices — Primitives}
	%----------------------------------------------------------
	
	\begin{exerciceblock}{Primitives successives de $\delta_0$}
		\hypertarget{exo:ch4:ex1}{}\label{exo:ch4:ex1}
		Trouver, pour chaque $n \in \{1, 2, 3\}$, la primitive d'ordre $n$ de $\delta_0$ qui s'annule sur $]-\infty, 0[$.
	\end{exerciceblock}
	\navtoCorr{ch4}{1}
	
	\begin{exerciceblock}{Résolution de $T' = H - 1$}
		\hypertarget{exo:ch4:ex2}{}\label{exo:ch4:ex2}
		Trouver la solution générale de $T' = H(x) - 1$ dans $\mathcal{D}'(\mathbb{R})$.
	\end{exerciceblock}
	\navtoCorr{ch4}{2}
	
	\begin{exerciceblock}{Équation de la chaleur et primitive en $t$}
		\hypertarget{exo:ch4:ex3}{}\label{exo:ch4:ex3}
		Soit $\Gamma(x,t) = \dfrac{1}{2\sqrt{\pi t}} e^{-x^2/(4t)}$ pour $t > 0$, $x \in \mathbb{R}$.
		
		Montrer que $\Gamma(\cdot, t) \xrightarrow{\mathcal{D}'} \delta_0$ lorsque $t \to 0^+$, et en déduire que la distribution $E(x,t) = H(t)\Gamma(x,t)$ vérifie $\partial_t E - \partial_x^2 E = \delta_{(0,0)}$ dans $\mathcal{D}'(\mathbb{R}^2)$.
	\end{exerciceblock}
	\navtoCorr{ch4}{3}
	
	\begin{exerciceblock}{Primitives de la valeur principale}
		\hypertarget{exo:ch4:ex4}{}\label{exo:ch4:ex4}
		Montrer que $x\,\mathrm{vp}\dfrac{1}{x} = 1$, et en déduire que la primitive de $1$ dans $\mathcal{D}'(\mathbb{R})$ est $x + C$.
	\end{exerciceblock}
	\navtoCorr{ch4}{4}
	
	\begin{exerciceblock}{Structure des primitives du peigne de Dirac}
		\hypertarget{exo:ch4:ex5}{}\label{exo:ch4:ex5}
		Déterminer la primitive particulière $\Sigma(x)$ du peigne de Dirac $\Delta_1 = \displaystyle\sum_{n\in\mathbb{Z}}\delta_n$ qui s'annule sur $]-\infty, 0[$.
	\end{exerciceblock}
	\navtoCorr{ch4}{5}
	
	%----------------------------------------------------------
	\begin{retenir}{Chapitre 4}
		\begin{itemize}
			\item $T' = 0$ dans $\mathcal{D}'(\mathbb{R})$ si et seulement si $T$ est une constante.
			\item Toute distribution admet une infinité de primitives, formant un espace affine de dimension $1$.
			\item La primitive d'ordre $n$ de $\delta_0$ s'annulant sur $]-\infty, 0[$ est $\dfrac{x^n}{n!}H(x)$.
			\item Les opérations de dérivation et de recherche de primitive dans $\mathcal{D}'$ sont inverses l'une de l'autre, modulo des constantes additives.
			\item La primitive de $\mathrm{vp}\frac{1}{x}$ est $\ln|x|$ ; celle de $-\mathrm{Pf}\frac{1}{x^2}$ est $\mathrm{vp}\frac{1}{x}$.
		\end{itemize}
	\end{retenir}
	
	% ============================================================
	% CHAPITRE 5 : OPÉRATIONS SUR LES DISTRIBUTIONS
	% ============================================================
	\chapter{Opérations sur les distributions}
	
	%----------------------------------------------------------
	\section{Multiplication par une fonction \texorpdfstring{$C^\infty$}{C infini}}
	%----------------------------------------------------------
	
	\begin{definition}[Produit d'une distribution par une fonction $C^\infty$]
		Soit $T \in \mathcal{D}'(\Omega)$ et $a \in C^\infty(\Omega)$. La forme linéaire $aT$ définie sur $C_0^\infty(\Omega)$ par
		\[
		\forall \varphi \in C_0^\infty(\Omega), \quad \langle aT, \varphi \rangle = \langle T, a\varphi \rangle
		\]
		est une distribution, appelée \emph{produit de $a$ par $T$}.
	\end{definition}
	
	\begin{remarque}
		La définition est cohérente car $a\varphi \in C_0^\infty(\Omega)$ dès que $a \in C^\infty(\Omega)$ et $\varphi \in C_0^\infty(\Omega)$. La continuité de $aT$ se vérifie via la formule de Leibniz.
	\end{remarque}
	
	Pour $a, b \in C^\infty(\Omega)$ et $T, S \in \mathcal{D}'(\Omega)$, on dispose des propriétés algébriques :
	\[
	(a+b)T = aT + bT, \qquad (ab)T = a(bT), \qquad a(S+T) = aS + aT.
	\]
	
	\begin{proposition}[Continuité du produit]
		Soient $T \in \mathcal{D}'(\Omega)$, $a \in C^\infty(\Omega)$. Si $(a_n)$ converge vers $a$ dans $C^\infty(\Omega)$ et $(T_n)$ converge vers $T$ dans $\mathcal{D}'(\Omega)$, alors $a_n T_n \to aT$ dans $\mathcal{D}'(\Omega)$.
	\end{proposition}
	
	\begin{exemple}
		\begin{itemize}
			\item Si $a \in C^\infty(\Omega)$ et $f \in L^1_{\mathrm{loc}}(\Omega)$, alors $a T_f = T_{af}$.
			\item Si $a \in C^\infty(\Omega)$ et $x_0 \in \Omega$, alors $a\delta_{x_0} = a(x_0)\delta_{x_0}$. En particulier, $x\delta_0 = 0$ dans $\mathcal{D}'(\mathbb{R})$.
			\item On a $x\,\mathrm{vp}\dfrac{1}{x} = 1$ dans $\mathcal{D}'(\mathbb{R})$.
		\end{itemize}
	\end{exemple}
	
	\begin{remarque}
		On ne peut pas définir de produit raisonnable entre deux distributions générales. En effet, si un tel produit était commutatif et associatif, on obtiendrait la contradiction $\delta_0 = 0$ en utilisant $x\delta_0 = 0$ et $x\,\mathrm{vp}\frac{1}{x}=1$.
	\end{remarque}
	
	%----------------------------------------------------------
	\section{Les équations \texorpdfstring{$xT=0$}{xT=0}, \texorpdfstring{$xT=1$}{xT=1} et \texorpdfstring{$xT=S$}{xT=S}}
	%----------------------------------------------------------
	
	\begin{proposition}
		Soit $T \in \mathcal{D}'(\mathbb{R})$. On a l'équivalence :
		\[
		xT = 0 \quad \Longleftrightarrow \quad T = C\delta_0, \quad C \in \mathbb{C}.
		\]
	\end{proposition}
	
	\begin{proof}
		Si $T = C\delta_0$, alors $\langle xT, \varphi \rangle = C\langle \delta_0, x\varphi \rangle = C \cdot 0 = 0$.
		
		Réciproquement, si $xT = 0$, pour toute $\psi \in C_0^\infty(\mathbb{R})$ vérifiant $\psi(0) = 0$, on peut écrire par Taylor $\psi(x) = x\theta(x)$ avec $\theta \in C_0^\infty(\mathbb{R})$, donc $\langle T, \psi \rangle = \langle xT, \theta \rangle = 0$. En fixant $\chi \in C_0^\infty(\mathbb{R})$ avec $\chi(0)=1$ et en décomposant $\varphi = \varphi(0)\chi + (\varphi - \varphi(0)\chi)$, on obtient $\langle T, \varphi \rangle = \varphi(0)\langle T, \chi \rangle$, d'où $T = C\delta_0$ avec $C = \langle T, \chi \rangle$.
	\end{proof}
	
	\begin{proposition}
		Les distributions $T \in \mathcal{D}'(\mathbb{R})$ vérifiant $xT = 1$ sont exactement
		\[
		T = \mathrm{vp}\frac{1}{x} + C\delta_0, \quad C \in \mathbb{C}.
		\]
	\end{proposition}
	
	\begin{proof}
		On sait que $x\,\mathrm{vp}\frac{1}{x} = 1$. Si $T$ est une solution, alors $x(T - \mathrm{vp}\frac{1}{x}) = 0$, donc $T - \mathrm{vp}\frac{1}{x} = C\delta_0$ par la proposition précédente.
	\end{proof}
	
	\begin{proposition}
		En dimension $d$ quelconque, soit $T \in \mathcal{D}'(\Omega)$ avec $0 \in \Omega$. Si $x_i T = 0$ pour tout $i \in \{1, \ldots, d\}$, alors $T = C\delta_0$ avec $C \in \mathbb{C}$.
	\end{proposition}
	
	%----------------------------------------------------------
	\section{Dérivation d'une distribution}
	%----------------------------------------------------------
	
	La dérivation est l'opération centrale de la théorie des distributions. Son principe est fondé sur la formule d'intégration par parties.
	
	\begin{definition}[Dérivée d'une distribution]
		Soit $T \in \mathcal{D}'(\Omega)$ et $i \in \{1, \ldots, d\}$. La forme linéaire $\partial_{x_i} T$ définie sur $C_0^\infty(\Omega)$ par
		\[
		\forall \varphi \in C_0^\infty(\Omega), \quad \langle \partial_{x_i} T, \varphi \rangle = -\langle T, \partial_{x_i} \varphi \rangle
		\]
		est une distribution sur $\Omega$, appelée \emph{$i$-ième dérivée partielle de $T$}.
		
		Plus généralement, pour tout multi-indice $\alpha \in \mathbb{N}^d$ :
		\[
		\langle \partial^\alpha T, \varphi \rangle = (-1)^{|\alpha|} \langle T, \partial^\alpha \varphi \rangle.
		\]
	\end{definition}
	
	\begin{remarque}
		Toute distribution est infiniment dérivable. Si $T$ est d'ordre $m$, alors $\partial_{x_i} T$ est d'ordre $m+1$.
	\end{remarque}
	
	\begin{proposition}[Continuité de la dérivation]
		Si $(T_n) \to T$ dans $\mathcal{D}'(\Omega)$, alors $\partial^\alpha T_n \to \partial^\alpha T$ dans $\mathcal{D}'(\Omega)$ pour tout $\alpha \in \mathbb{N}^d$.
	\end{proposition}
	
	\begin{proposition}[Règle de Leibniz]
		Pour $a \in C^\infty(\Omega)$ et $T \in \mathcal{D}'(\Omega)$ :
		\[
		\partial_{x_i}(aT) = (\partial_{x_i} a)\,T + a\,\partial_{x_i} T.
		\]
	\end{proposition}
	
	\begin{exemple}
		\begin{itemize}
			\item Soit $H$ la fonction de Heaviside ($H(x) = 0$ pour $x < 0$, $H(x) = 1$ pour $x > 0$). Alors $H' = \delta_0$ dans $\mathcal{D}'(\mathbb{R})$.
			\item On a $(\ln|x|)' = \mathrm{vp}\dfrac{1}{x}$ dans $\mathcal{D}'(\mathbb{R})$.
			\item Pour $f \in L^1_{\mathrm{loc}}(\mathbb{R})$, la fonction $F(x) = \int_0^x f(t)\,dt$ vérifie $F' = T_f$ dans $\mathcal{D}'(\mathbb{R})$.
		\end{itemize}
	\end{exemple}
	
	%----------------------------------------------------------
	\section{Les équations \texorpdfstring{$T'=0$}{T'=0} et \texorpdfstring{$\partial_{x_i} T = 0$}{dxi T = 0}}
	%----------------------------------------------------------
	
	\begin{proposition}
		Soit $T \in \mathcal{D}'(\mathbb{R})$. Alors $T' = 0$ si et seulement si $T$ est une distribution associée à une constante.
	\end{proposition}
	
	\begin{corollary}
		Soit $T \in \mathcal{D}'(\Omega)$ où $\Omega$ est un ouvert connexe de $\mathbb{R}^d$. Si $\partial_{x_i} T = 0$ pour tout $i$, alors $T$ est constante.
	\end{corollary}
	
	%----------------------------------------------------------
	\section{Formule des sauts en dimension 1}
	%----------------------------------------------------------
	
	\begin{theorem}[Formule des sauts]
		Soit $f$ une fonction $C^1$ par morceaux sur $[a,b]$ admettant des limites à gauche et à droite en chaque discontinuité $a_1 < a_2 < \cdots < a_n$ (avec $a_0 = a$, $a_{n+1} = b$). Alors
		\[
		(T_f)' = T_{f'} + \sum_{i=0}^{n+1} \bigl(f(a_i^+) - f(a_i^-)\bigr)\,\delta_{a_i},
		\]
		où $f' $ est la dérivée classique définie presque partout, et les conventions $f(a_0^-) = 0$, $f(a_{n+1}^+) = 0$.
	\end{theorem}
	
	\begin{proof}
		Par définition, $\langle (T_f)', \varphi \rangle = -\int_a^b f(x)\varphi'(x)\,dx$. On découpe cette intégrale sur chaque intervalle $[a_i, a_{i+1}]$ et on intègre par parties sur chacun, faisant apparaître des termes de bord correspondant aux sauts de $f$ aux points $a_i$.
	\end{proof}
	
	\begin{proposition}
		Soit $u \in C^1([a,b])$. Si $\tilde{u}$ désigne son prolongement par $0$ hors de $[a,b]$, alors
		\[
		(T_{\tilde{u}})' = T_{u'} + u(a)\delta_a - u(b)\delta_b.
		\]
	\end{proposition}
	
	%----------------------------------------------------------
	\section{Exemples illustratifs}
	%----------------------------------------------------------
	
	\begin{exemple}[Dérivée de $|x|$]
		La fonction $|x|$ est continue sur $\mathbb{R}$ et de classe $C^1$ sur $\mathbb{R}^* = \mathbb{R} \setminus \{0\}$. Sa dérivée classique est $\mathrm{sgn}(x)$ (avec $+1$ pour $x>0$ et $-1$ pour $x<0$). Comme $|x|$ est continue en $0$ (pas de saut), la formule des sauts donne
		\[
		|x|' = \mathrm{sgn}(x) \quad \text{dans } \mathcal{D}'(\mathbb{R}).
		\]
	\end{exemple}
	
	\begin{exemple}[Dérivée de $x_+ = \max(x,0)$]
		La fonction $x_+ = xH(x)$ vaut $0$ pour $x \leq 0$ et $x$ pour $x > 0$. Par la règle de Leibniz :
		\[
		x_+' = H(x) + x\delta_0 = H(x).
		\]
		On retrouve que $x_+'' = \delta_0$.
	\end{exemple}
	
	\begin{exemple}[Dérivée de la valeur absolue, méthode directe]
		Pour $\varphi \in \mathcal{D}(\mathbb{R})$ :
		\[
		\langle |x|', \varphi \rangle = -\int_{-\infty}^{+\infty} |x| \varphi'(x)\,dx = \int_{-\infty}^0 x\varphi'(x)\,dx - \int_0^{+\infty} x\varphi'(x)\,dx.
		\]
		Une intégration par parties dans chaque intégrale donne $\langle |x|', \varphi \rangle = \int_{-\infty}^0 (-\varphi)\,dx + \int_0^{+\infty} \varphi\,dx$, ce qui correspond bien à $\mathrm{sgn}(x)$.
	\end{exemple}
	
	%----------------------------------------------------------
	\section{QCM — Opérations sur les distributions}
	%----------------------------------------------------------
	
	\begin{qcmblock}
		\hypertarget{qcm:ch5:start}{}\label{qcm:ch5:start}
		\hypertarget{qcm:ch5:q1}{}\label{qcm:ch5:q1}
		\textbf{Question 1.} Soit $a \in C^\infty(\mathbb{R})$. Comment est défini $\langle aT, \varphi \rangle$ ?\\
		A. $\langle T, a\varphi \rangle$\quad B. $\langle T, \varphi/a \rangle$\quad C. $a\langle T, \varphi \rangle$\quad D. $\langle aT', \varphi \rangle$\\
		\navtoQCMRep{ch5}{1}
		
		\medskip
		\hypertarget{qcm:ch5:q2}{}\label{qcm:ch5:q2}
		\textbf{Question 2.} Quelle est la valeur de $x\delta_0$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $\delta_0$\quad B. $\delta_0'$\quad C. $0$\quad D. $1$\\
		\navtoQCMRep{ch5}{2}
		
		\medskip
		\hypertarget{qcm:ch5:q3}{}\label{qcm:ch5:q3}
		\textbf{Question 3.} Les solutions de $xT = 0$ dans $\mathcal{D}'(\mathbb{R})$ sont :\\
		A. $T = 0$\quad B. $T = C\delta_0$, $C \in \mathbb{C}$\quad C. $T = \mathrm{vp}\frac{1}{x}$\quad D. $T = H$\\
		\navtoQCMRep{ch5}{3}
		
		\medskip
		\hypertarget{qcm:ch5:q4}{}\label{qcm:ch5:q4}
		\textbf{Question 4.} Si $T' = 5$ dans $\mathcal{D}'(\mathbb{R})$, quelle est la forme générale de $T$ ?\\
		A. $T = 5x$\quad B. $T = 5\delta_0$\quad C. $T = 5x + C$, $C \in \mathbb{C}$\quad D. $T = 5H$\\
		\navtoQCMRep{ch5}{4}
		
		\medskip
		\hypertarget{qcm:ch5:q5}{}\label{qcm:ch5:q5}
		\textbf{Question 5.} Soit $f(x) = |x-1|$. Quelle est $f'$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $\mathrm{sgn}(x-1) + 2\delta_1$\quad B. $\mathrm{sgn}(x-1)$\quad C. $|x-1|$\quad D. $\delta_1$\\
		\navtoQCMRep{ch5}{5}
		
		\medskip
		\hypertarget{qcm:ch5:q6}{}\label{qcm:ch5:q6}
		\textbf{Question 6.} La dérivée de $H(x) - H(x-1)$ dans $\mathcal{D}'(\mathbb{R})$ est :\\
		A. $\delta_0 - \delta_1$\quad B. $\delta_0 + \delta_1$\quad C. $2\delta_0$\quad D. $0$\\
		\navtoQCMRep{ch5}{6}
		
		\medskip
		\hypertarget{qcm:ch5:q7}{}\label{qcm:ch5:q7}
		\textbf{Question 7.} Laquelle de ces opérations est \emph{toujours} bien définie dans $\mathcal{D}'$ ?\\
		A. $T \cdot S$ (produit de deux distributions)\quad B. $a \cdot T$ avec $a \in C^\infty$\quad C. $1/T$\quad D. $|T|$\\
		\navtoQCMRep{ch5}{7}
		
		\medskip
		\hypertarget{qcm:ch5:q8}{}\label{qcm:ch5:q8}
		\textbf{Question 8.} Quelle est la dérivée de $f(x) = e^x H(x)$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $e^x H(x)$\quad B. $e^x H(x) + \delta_0$\quad C. $\delta_0$\quad D. $e^x$\\
		\navtoQCMRep{ch5}{8}
		
		\medskip
		\hypertarget{qcm:ch5:q9}{}\label{qcm:ch5:q9}
		\textbf{Question 9.} Soit $T \in \mathcal{D}'(\mathbb{R})$ d'ordre $m$. Quel est l'ordre de $T'$ ?\\
		A. $m$\quad B. $m-1$\quad C. $m+1$\quad D. $2m$\\
		\navtoQCMRep{ch5}{9}
		
		\medskip
		\hypertarget{qcm:ch5:q10}{}\label{qcm:ch5:q10}
		\textbf{Question 10.} Quelle propriété caractérise les solutions de $T'' = 0$ dans $\mathcal{D}'(\mathbb{R})$ ?\\
		A. $T$ est nulle\quad B. $T$ est affine : $T = ax + b$\quad C. $T = \delta_0$\quad D. $T$ est $2\pi$-périodique\\
		\navtoQCMRep{ch5}{10}
	\end{qcmblock}
	%----------------------------------------------------------
	\section{Exercices — Opérations sur les distributions}
	%----------------------------------------------------------
	
	\begin{exerciceblock}{Calcul de dérivées par la définition}
		\hypertarget{exo:ch5:ex1}{}\label{exo:ch5:ex1}
		Calculer dans $\mathcal{D}'(\mathbb{R})$ les dérivées des distributions $T_f$ associées aux fonctions suivantes :
		\begin{enumerate}[label=(\alph*)]
			\item $f(x) = \mathrm{sgn}(x)$.
			\item $f(x) = |x|$.
			\item $f(x) = e^x H(x)$.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch5}{1}
	
	\begin{exerciceblock}{Équations algébriques dans $\mathcal{D}'(\mathbb{R})$}
		\hypertarget{exo:ch5:ex2}{}\label{exo:ch5:ex2}
		Résoudre dans $\mathcal{D}'(\mathbb{R})$ les équations suivantes :
		\begin{enumerate}[label=(\alph*)]
			\item $xT = 0$.
			\item $xT = 1$.
			\item $x^2 T = \delta_0$.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch5}{2}
	
	\begin{exerciceblock}{Formule des sauts et dérivées successives}
		\hypertarget{exo:ch5:ex3}{}\label{exo:ch5:ex3}
		Soit $f : \mathbb{R} \to \mathbb{R}$ définie par $f(x) = \sin(x)$ pour $x \in [0, \pi]$ et $f(x) = 0$ sinon.
		\begin{enumerate}[label=(\alph*)]
			\item Calculer $f'$ dans $\mathcal{D}'(\mathbb{R})$.
			\item Calculer $f''$ dans $\mathcal{D}'(\mathbb{R})$.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch5}{3}
	
	\begin{exerciceblock}{Produit et multiplication $C^\infty$}
		\hypertarget{exo:ch5:ex4}{}\label{exo:ch5:ex4}
		Soit $a(x) = x^2$ et $T = \mathrm{vp}\dfrac{1}{x} \in \mathcal{D}'(\mathbb{R})$.
		\begin{enumerate}[label=(\alph*)]
			\item Calculer $x^2\,\mathrm{vp}\dfrac{1}{x}$ dans $\mathcal{D}'(\mathbb{R})$.
			\item En déduire $(x^2\,\mathrm{vp}\dfrac{1}{x})'$ par deux méthodes : directement et via la règle de Leibniz.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch5}{4}
	
	\begin{exerciceblock}{Distribution à support réduit à un point}
		\hypertarget{exo:ch5:ex5}{}\label{exo:ch5:ex5}
		On admet que si $T \in \mathcal{D}'(\mathbb{R})$ est à support $\{0\}$, alors il existe $m \in \mathbb{N}$ et $a_0, \ldots, a_m \in \mathbb{C}$ tels que $T = \sum_{k=0}^m a_k \delta_0^{(k)}$.
		
		Déterminer toutes les distributions $T \in \mathcal{D}'(\mathbb{R})$ à support dans $\{0\}$ qui vérifient $x^2 T = 0$.
	\end{exerciceblock}
	\navtoCorr{ch5}{5}
	
	%----------------------------------------------------------
	\begin{retenir}{Chapitre 5}
		\begin{itemize}
			\item Le produit d'une distribution par $a \in C^\infty$ est défini par $\langle aT, \varphi \rangle = \langle T, a\varphi \rangle$.
			\item On ne peut pas définir le produit de deux distributions générales.
			\item Toute distribution est infiniment dérivable : $\langle T', \varphi \rangle = -\langle T, \varphi' \rangle$.
			\item La dérivation est continue : $T_n \to T \Rightarrow T_n' \to T'$.
			\item Formule des sauts : $(T_f)' = T_{f'} + \sum_i \bigl(f(a_i^+) - f(a_i^-)\bigr)\delta_{a_i}$.
			\item $T' = 0 \Leftrightarrow T$ est constante sur tout ouvert connexe.
			\item Les solutions de $xT = 0$ sont $T = C\delta_0$ ; celles de $xT = 1$ sont $T = \mathrm{vp}\frac{1}{x} + C\delta_0$.
		\end{itemize}
	\end{retenir}
	
	% ============================================================
	% CHAPITRE 6 : CONVOLUTION DES DISTRIBUTIONS
	% ============================================================
	\chapter{Convolution des distributions}
	
	%----------------------------------------------------------
	\section{Produit de convolution de deux distributions}
	%----------------------------------------------------------
	
	Rappelons d'abord le produit de convolution dans le cadre $L^1$. Si $u \in L^1(\mathbb{R}^d)$ et $\varphi \in C_0^\infty(\mathbb{R}^d)$, alors pour tout $x \in \mathbb{R}^d$ :
	\[
	(u * \varphi)(x) = \int_{\mathbb{R}^d} u(y)\varphi(x-y)\,dy = \langle T_u, \varphi(x - \cdot) \rangle.
	\]
	Cette formule fournit la définition naturelle dans le cadre distributionnel.
	
	\begin{definition}[Convolution distribution-fonction test]
		Soient $T \in \mathcal{D}'(\mathbb{R}^d)$ et $\varphi \in C_0^\infty(\mathbb{R}^d)$. Leur \emph{produit de convolution} est la fonction définie par
		\[
		\forall x \in \mathbb{R}^d, \quad (T * \varphi)(x) = \langle T, \varphi(x - \cdot) \rangle.
		\]
		Alors $T * \varphi \in C^\infty(\mathbb{R}^d)$.
	\end{definition}
	
	\begin{proposition}[Dérivation et convolution]
		Pour $T \in \mathcal{D}'(\mathbb{R}^d)$, $\varphi \in C_0^\infty(\mathbb{R}^d)$ et $\alpha \in \mathbb{N}^d$ :
		\[
		\partial^\alpha(T * \varphi) = (\partial^\alpha T) * \varphi = T * (\partial^\alpha \varphi).
		\]
		Plus généralement, pour toute décomposition $\alpha = \alpha_1 + \alpha_2$ :
		\[
		\partial^\alpha(T * \varphi) = (\partial^{\alpha_1} T) * (\partial^{\alpha_2} \varphi).
		\]
	\end{proposition}
	
	On convient de noter $\check{f}(x) = f(-x)$ pour toute fonction $f$. Si $\varphi \in C_0^\infty(\mathbb{R}^d)$, on a la relation utile :
	\[
	\langle T, \varphi \rangle = (T * \check{\varphi})(0).
	\]
	
	\begin{proposition}
		Pour $T \in \mathcal{D}'(\mathbb{R}^d)$ et $\varphi, \psi \in C_0^\infty(\mathbb{R}^d)$ :
		\[
		(T * \varphi) * \psi = T * (\varphi * \psi) \quad \text{et} \quad \langle T * \varphi, \psi \rangle = \langle T, \check{\varphi} * \psi \rangle.
		\]
	\end{proposition}
	
	\begin{theorem}[Densité de $C_0^\infty$ dans $\mathcal{D}'$]
		Soient $\Omega$ un ouvert de $\mathbb{R}^d$ et $T \in \mathcal{D}'(\Omega)$. Il existe une suite $(\varphi_n) \subset C_0^\infty(\Omega)$ qui converge vers $T$ au sens des distributions.
	\end{theorem}
	
	\begin{definition}[Convolution de deux distributions]
		Soient $T \in \mathcal{D}'(\mathbb{R}^d)$ et $S \in \mathcal{D}'(\mathbb{R}^d)$ à \emph{support compact}. On approche $T$ par $(\varphi_n) \subset C_0^\infty(\mathbb{R}^d)$. Alors $(\varphi_n * S)$ converge dans $\mathcal{D}'(\mathbb{R}^d)$ vers une limite notée $T * S$.
	\end{definition}
	
	%----------------------------------------------------------
	\section{Propriétés de la convolution}
	%----------------------------------------------------------
	
	\begin{proposition}[Propriétés algébriques]
		Soient $T \in \mathcal{D}'(\mathbb{R}^d)$ et $S, U \in \mathcal{D}'(\mathbb{R}^d)$ à supports compacts :
		\begin{enumerate}[label=(\arabic*)]
			\item \textbf{Associativité :} $T * (S * U) = (T * S) * U$.
			\item \textbf{Commutativité :} $T * S = S * T$.
			\item \textbf{Élément neutre :} $T * \delta_0 = \delta_0 * T = T$.
		\end{enumerate}
	\end{proposition}
	
	\begin{proposition}[Dérivation et convolution de distributions]
		Soient $T \in \mathcal{D}'(\mathbb{R}^d)$ et $S \in \mathcal{D}'(\mathbb{R}^d)$ à support compact, $\alpha \in \mathbb{N}^d$. Alors
		\[
		\partial^\alpha(T * S) = (\partial^\alpha T) * S = T * (\partial^\alpha S).
		\]
		En particulier, $(\partial^\alpha \delta_0) * T = \partial^\alpha T$.
	\end{proposition}
	
	\begin{proposition}[Continuité]
		\begin{enumerate}[label=(\arabic*)]
			\item Si $(T_n) \to T$ dans $\mathcal{D}'(\mathbb{R}^d)$ et $\varphi \in C_0^\infty$, alors $T_n * \varphi \to T * \varphi$ dans $\mathcal{D}'$.
			\item Si $(\varphi_n) \to \varphi$ dans $C_0^\infty$ et $T \in \mathcal{D}'$, alors $T * \varphi_n \to T * \varphi$ dans $\mathcal{D}'$.
		\end{enumerate}
	\end{proposition}
	
	%----------------------------------------------------------
	\section{Interprétation physique de la convolution}
	%----------------------------------------------------------
	
	Considérons un système physique linéaire, invariant par translation temporelle, qui transforme un signal d'entrée $s(t)$ en une réponse $r(t)$. Les hypothèses physiques sont :
	\begin{itemize}
		\item \textbf{Superposition :} la réponse à $\lambda_1 s_1 + \lambda_2 s_2$ est $\lambda_1 r_1 + \lambda_2 r_2$.
		\item \textbf{Invariance temporelle :} un signal décalé de $\tau$ produit une réponse décalée de $\tau$.
		\item \textbf{Continuité :} des signaux voisins produisent des réponses voisines.
	\end{itemize}
	
	Sous ces hypothèses, il existe une distribution $T$ (la \emph{réponse impulsionnelle} du système) telle que $r = T * s$. Ce résultat explique l'omniprésence de la convolution en traitement du signal et en physique.
	
	%----------------------------------------------------------
	\section{Comment calculer un produit de convolution}
	%----------------------------------------------------------
	
	\subsection*{Cas 1 : Convolution de deux fonctions dans $L^1_{\mathrm{loc}}(\mathbb{R}^d)$}
	
	Si $f, g \in L^1_{\mathrm{loc}}(\mathbb{R}^d)$, la convolution (quand elle est définie) est donnée par la formule intégrale classique :
	\[
	(f * g)(x) = \int_{\mathbb{R}^d} f(x-y)\,g(y)\,dy.
	\]
	
	\subsection*{Cas 2 : Convolution d'une distribution et d'une fonction $C_0^\infty$}
	
	Si $T \in \mathcal{D}'(\mathbb{R}^d)$ et $\varphi \in C_0^\infty(\mathbb{R}^d)$, la distribution $T * \varphi$ est la fonction $C^\infty$ :
	\[
	x \longmapsto \langle T, \varphi(x - \cdot) \rangle.
	\]
	
	\subsection*{Cas 3 : Utilisation des propriétés de dérivation}
	
	En dimension $d = 1$, si $T = \tilde{T}'$ pour une distribution $\tilde{T}$ plus simple à convoluer, on calcule d'abord $\tilde{T} * S$ puis on utilise :
	\[
	T * S = (\tilde{T} * S)'.
	\]
	Réciproquement, si on connaît $T' * S$, on peut retrouver $T * S$ par une primitive (à une constante près).
	
	\begin{exemple}
		Calculons $\delta_0' * \delta_0'$. En utilisant la règle de dérivation : $\delta_0' * \delta_0' = (\delta_0 * \delta_0)'' = \delta_0'' $.
	\end{exemple}
	
	\begin{exemple}[Convolution avec le Dirac]
		Pour toute $T \in \mathcal{D}'(\mathbb{R}^d)$ et tout $a \in \mathbb{R}^d$, on a $T * \delta_a = \tau_a T$ (le translaté de $T$ en $a$), où $\langle \tau_a T, \varphi \rangle = \langle T, \varphi(\cdot + a) \rangle$. En particulier $T * \delta_0 = T$.
	\end{exemple}
	
	\begin{exemple}[Convolution de deux fonctions de Heaviside]
		Calculons $H * H$ dans $\mathcal{D}'(\mathbb{R})$. Pour $x > 0$ :
		\[
		(H * H)(x) = \int_{-\infty}^{+\infty} H(x-y)H(y)\,dy = \int_0^x 1\,dy = x.
		\]
		Pour $x < 0$, l'intégrale est nulle. Donc $H * H = x_+ = xH(x)$.
	\end{exemple}
	
	%----------------------------------------------------------
	\section{Exemples de calculs de convolution}
	%----------------------------------------------------------
	
	\begin{exemple}[$\delta_0 * T = T$]
		Pour $\varphi \in C_0^\infty(\mathbb{R}^d)$ :
		\[
		(\delta_0 * T)(x) = \langle \delta_0, \varphi(x - \cdot) \rangle = \varphi(x - 0) = \varphi(x) = \langle T, \varphi(x-\cdot) \rangle \big|_{T=\delta_0}.
		\]
		Plus directement : $\langle \delta_0 * T, \psi \rangle = \langle T, \check{\delta_0} * \psi \rangle = \langle T, \psi \rangle$.
	\end{exemple}
	
	\begin{exemple}[$H * \delta_0' = H' = \delta_0$]
		Par la propriété de dérivation : $H * \delta_0' = (H * \delta_0)' = H' = \delta_0$.
	\end{exemple}
	
	%----------------------------------------------------------
	\section{QCM — Convolution des distributions}
	%----------------------------------------------------------
	\begin{qcmblock}
		\hypertarget{qcm:ch6:start}{}\label{qcm:ch6:start}
		\hypertarget{qcm:ch6:q1}{}\label{qcm:ch6:q1}
		\textbf{Question 1.} Quel est l'élément neutre de la convolution dans $\mathcal{D}'(\mathbb{R}^d)$ ?\\
		A. $0$\quad B. $1$\quad C. $\delta_0$\quad D. $H$\\
		\navtoQCMRep{ch6}{1}
		
		\medskip
		\hypertarget{qcm:ch6:q2}{}\label{qcm:ch6:q2}
		\textbf{Question 2.} La convolution $T * S$ est-elle toujours définie pour $T, S \in \mathcal{D}'(\mathbb{R}^d)$ ?\\
		A. Oui\quad B. Non, il faut une hypothèse sur les supports\quad C. Seulement si $T$ est une fonction\quad D. Seulement en dimension 1\\
		\navtoQCMRep{ch6}{2}
		
		\medskip
		\hypertarget{qcm:ch6:q3}{}\label{qcm:ch6:q3}
		\textbf{Question 3.} Quelle est la valeur de $\delta_0' * T$ ?\\
		A. $T$\quad B. $T'$\quad C. $T + \delta_0$\quad D. $0$\\
		\navtoQCMRep{ch6}{3}
		
		\medskip
		\hypertarget{qcm:ch6:q4}{}\label{qcm:ch6:q4}
		\textbf{Question 4.} Si $(f * g)(x) = \int_{\mathbb{R}} f(y)g(x-y)\,dy$, la convolution de $H$ avec elle-même $H * H$ vaut :\\
		A. $H$\quad B. $2H$\quad C. $xH(x)$\quad D. $H(x)/2$\\
		\navtoQCMRep{ch6}{4}
		
		\medskip
		\hypertarget{qcm:ch6:q5}{}\label{qcm:ch6:q5}
		\textbf{Question 5.} Quelle relation lie $\partial^\alpha(T*S)$, $(\partial^\alpha T)*S$ et $T*(\partial^\alpha S)$ ?\\
		A. $\partial^\alpha(T*S) = (\partial^\alpha T)*S = T*(\partial^\alpha S)$\\
		B. $\partial^\alpha(T*S) = (\partial^\alpha T)*S$ seulement\\
		C. Il n'y a pas de relation simple\\
		D. $\partial^\alpha(T*S) = 0$\\
		\navtoQCMRep{ch6}{5}
		
		\medskip
		\hypertarget{qcm:ch6:q6}{}\label{qcm:ch6:q6}
		\textbf{Question 6.} Quel résultat de densité est essentiel pour définir $T * S$ lorsque $S$ est à support compact ?\\
		A. $C_0^\infty(\mathbb{R}^d)$ est dense dans $L^2$\\
		B. $C_0^\infty(\mathbb{R}^d)$ est dense dans $\mathcal{D}'$ pour la topologie des distributions\\
		C. $C_0^\infty(\mathbb{R}^d)$ est dense dans les fonctions continues\\
		D. $C_0^\infty(\mathbb{R}^d)$ est dense dans $L^1$\\
		\navtoQCMRep{ch6}{6}
		
		\medskip
		\hypertarget{qcm:ch6:q7}{}\label{qcm:ch6:q7}
		\textbf{Question 7.} Que vaut $\delta_0^{(n)} * \delta_0^{(m)}$ ?\\
		A. $\delta_0^{(n+m)}$\quad B. $\delta_0^{(nm)}$\quad C. $0$\quad D. $n!\,m!\,\delta_0$\\
		\navtoQCMRep{ch6}{7}
		
		\medskip
		\hypertarget{qcm:ch6:q8}{}\label{qcm:ch6:q8}
		\textbf{Question 8.} Que vaut $(H * \delta_1)(x)$ où $\delta_1 = \delta_{x=1}$ ?\\
		A. $H(x)$\quad B. $H(x-1)$\quad C. $H(x+1)$\quad D. $H(x) + 1$\\
		\navtoQCMRep{ch6}{8}
		
		\medskip
		\hypertarget{qcm:ch6:q9}{}\label{qcm:ch6:q9}
		\textbf{Question 9.} L'interprétation physique de la convolution correspond à :\\
		A. La réponse d'un système non linéaire\\
		B. La réponse impulsionnelle d'un système linéaire invariant par translation\\
		C. La transformée de Fourier du signal\\
		D. La primitive du signal d'entrée\\
		\navtoQCMRep{ch6}{9}
		
		\medskip
		\hypertarget{qcm:ch6:q10}{}\label{qcm:ch6:q10}
		\textbf{Question 10.} Que vaut $\delta_0 * \delta_0$ ?\\
		A. $1$\quad B. $\delta_0^2$\quad C. $\delta_0$\quad D. $0$\\
		\navtoQCMRep{ch6}{10}
	\end{qcmblock}
	%----------------------------------------------------------
	\section{Exercices — Convolution des distributions}
	%----------------------------------------------------------
	
	\begin{exerciceblock}{Convolution et dérivation}
		\hypertarget{exo:ch6:ex1}{}\label{exo:ch6:ex1}
		\begin{enumerate}[label=(\alph*)]
			\item Calculer $\delta_0^{(n)} * H$ pour $n \geq 1$.
			\item Montrer que $H * H * \cdots * H$ ($n$ fois) vaut $\dfrac{x^{n-1}}{(n-1)!}H(x)$.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch6}{1}
	
	\begin{exerciceblock}{Convolution avec des translations de Dirac}
		\hypertarget{exo:ch6:ex2}{}\label{exo:ch6:ex2}
		Soient $a, b \in \mathbb{R}$. Calculer $\delta_a * \delta_b$ et $\delta_a * f$ pour $f \in L^1_{\mathrm{loc}}(\mathbb{R})$.
	\end{exerciceblock}
	\navtoCorr{ch6}{2}
	
	\begin{exerciceblock}{Calcul explicite de produits de convolution}
		\hypertarget{exo:ch6:ex3}{}\label{exo:ch6:ex3}
		Calculer dans $\mathcal{D}'(\mathbb{R})$ les convolutions suivantes :
		\begin{enumerate}[label=(\alph*)]
			\item $e^x H(x) * H(x)$.
			\item $H(x) * \cos(x)H(x)$.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch6}{3}
	
	\begin{exerciceblock}{Convolution et la propriété de régularisation}
		\hypertarget{exo:ch6:ex4}{}\label{exo:ch6:ex4}
		Soit $T \in \mathcal{D}'(\mathbb{R})$ et $\rho_\varepsilon(x) = \frac{1}{\varepsilon}\rho\bigl(\frac{x}{\varepsilon}\bigr)$ avec $\rho \in C_0^\infty(\mathbb{R})$, $\rho \geq 0$, $\int \rho = 1$ (noyau régularisant).
		\begin{enumerate}[label=(\alph*)]
			\item Montrer que $T * \rho_\varepsilon \in C^\infty(\mathbb{R})$.
			\item Montrer que $T * \rho_\varepsilon \to T$ dans $\mathcal{D}'(\mathbb{R})$ lorsque $\varepsilon \to 0$.
			\item Application : montrer que $f * \rho_\varepsilon \to f$ dans $L^1_{\mathrm{loc}}(\mathbb{R})$ pour $f \in L^1_{\mathrm{loc}}(\mathbb{R})$.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch6}{4}
	
	\begin{exerciceblock}{Convolution et résolution de l'équation $T' - T = \delta_0$}
		\hypertarget{exo:ch6:ex5}{}\label{exo:ch6:ex5}
		Trouver toutes les solutions $T \in \mathcal{D}'(\mathbb{R})$ de l'équation $T' - T = \delta_0$.
	\end{exerciceblock}
	\navtoCorr{ch6}{5}
	
	%----------------------------------------------------------
	\begin{retenir}{Chapitre 6}
		\begin{itemize}
			\item La convolution $T * \varphi$ pour $T \in \mathcal{D}'$ et $\varphi \in C_0^\infty$ est la fonction $C^\infty$ : $x \mapsto \langle T, \varphi(x-\cdot) \rangle$.
			\item La dérivation et la convolution commutent : $\partial^\alpha(T*S) = (\partial^\alpha T)*S = T*(\partial^\alpha S)$.
			\item $\delta_0$ est l'élément neutre : $T * \delta_0 = T$ pour toute distribution $T$.
			\item $\delta_0^{(n)} * T = T^{(n)}$ : la convolution par une dérivée de Dirac dérive.
			\item $\delta_a * T = \tau_a T$ : la convolution par $\delta_a$ translate de $a$.
			\item $\delta_a * \delta_b = \delta_{a+b}$ : la convolution des masses de Dirac correspond à l'addition des points.
			\item La convolution par un noyau régularisant $\rho_\varepsilon$ régularise : $T * \rho_\varepsilon \in C^\infty$ et $T * \rho_\varepsilon \to T$ dans $\mathcal{D}'$.
			\item La réponse d'un système linéaire invariant par translation est donnée par une convolution : $r = T * s$.
		\end{itemize}
	\end{retenir}
	
	% ============================================================
	% CHAPITRE 7 : APPLICATIONS AUX ÉQUATIONS DIFFÉRENTIELLES LINÉAIRES
	% ============================================================
	\chapter{Applications aux équations différentielles linéaires}
	
	%----------------------------------------------------------
	\section{Cadre général}
	%----------------------------------------------------------
	
	\begin{definition}[Opérateur différentiel à coefficients constants]
		Soit $P \in \mathbb{C}[X_1,\ldots,X_d]$ un polynôme de degré $m$,
		$P = \sum_{|\alpha|\leq m} a_\alpha X^\alpha$.
		L'opérateur différentiel associé est
		\[
		P(\partial) : T \;\longmapsto\; P(\partial)T
		:= \sum_{|\alpha|\leq m} a_\alpha\,\partial^\alpha T.
		\]
		Une \textbf{EDP linéaire à coefficients constants} est une équation de la forme
		$P(\partial)T = F$ avec $F \in \mathcal{D}'(\Omega)$ donné et
		$T \in \mathcal{D}'(\Omega)$ inconnu.
	\end{definition}
	
	\begin{proposition}[Structure de l'ensemble des solutions]
		L'ensemble des solutions de $P(\partial)T = F$ est soit vide, soit un
		\textbf{sous-espace affine} $T_0 + \ker P(\partial)$, où $T_0$ est une solution
		particulière et $\ker P(\partial) = \{T \in \mathcal{D}'(\Omega) : P(\partial)T = 0\}$.
	\end{proposition}
	
	\begin{remark}[À retenir]
		\begin{itemize}[itemsep=4pt]
			\item $P(\partial)$ agit linéairement sur $\mathcal{D}'(\Omega)$ : les distributions
			généralisent le cadre classique.
			\item La structure affine des solutions est identique à celle de l'algèbre linéaire :
			solution générale $=$ solution particulière $+$ noyau.
		\end{itemize}
	\end{remark}
	
	%----------------------------------------------------------
	\section{Solution fondamentale}
	%----------------------------------------------------------
	
	\begin{definition}[Solution fondamentale (élémentaire)]
		Une distribution $E \in \mathcal{D}'(\mathbb{R}^d)$ est une \textbf{solution fondamentale}
		de $P(\partial)$ si
		\[
		P(\partial)E = \delta_0.
		\]
		On dit aussi \emph{solution élémentaire} ou \emph{fonction de Green} de $P(\partial)$.
	\end{definition}
	
	\begin{theorem}[Théorème fondamental — Malgrange–Ehrenpreis (1955)]
		Tout opérateur différentiel à coefficients constants $P(\partial) \neq 0$ admet une
		\textbf{solution fondamentale} dans $\mathcal{D}'(\mathbb{R}^d)$.
	\end{theorem}
	
	\textit{Remarque.} La démonstration utilise la transformée de Fourier et des propriétés
	des fonctions holomorphes. Elle est admise dans ce cours.
	
	\begin{theorem}[Résolution par convolution]
		Soit $E$ une solution fondamentale de $P(\partial)$, et soit
		$F \in \mathcal{D}'(\mathbb{R}^d)$ à \textbf{support compact}. Alors $u := E * F$ est une
		solution de $P(\partial)u = F$.
	\end{theorem}
	
	\begin{proof}
		En utilisant le fait que la dérivation commute avec la convolution :
		\[
		P(\partial)(E * F) = \bigl(P(\partial)E\bigr) * F = \delta_0 * F = F.
		\]
	\end{proof}
	
	\begin{theorem}[Unicité et régularité]
		\begin{enumerate}[label=(\roman*), itemsep=4pt]
			\item Parmi les solutions à support compact, $E * F$ est l'\textbf{unique solution}
			de $P(\partial)u = F$.
			\item Si $E \in C^\infty(\mathbb{R}^d \setminus \{0\})$ et si $F \in C^\infty(\Omega)$,
			alors toute solution $T \in \mathcal{D}'(\Omega)$ de $P(\partial)T = T_F$
			est en fait une fonction $C^\infty$ \emph{(régularité elliptique)}.
		\end{enumerate}
	\end{theorem}
	
	\begin{remark}[À retenir]
		C'est là la force des distributions : une \textbf{solution fondamentale} $E$ concentre
		toute l'information du système. La formule $u = E * F$ signifie que l'on superpose
		les réponses impulsionnelles pondérées par la source $F$ — c'est l'intuition physique
		de la convolution. Le théorème de Malgrange–Ehrenpreis garantit l'\textbf{existence}
		de $E$ sans la calculer : on la cherche au cas par cas.
	\end{remark}
	
	%----------------------------------------------------------
	\section{Exemples fondamentaux}
	%----------------------------------------------------------
	
	\begin{example}[Équation de transport : $u' - \lambda u = f$]
		\textbf{Opérateur :} $P(\partial) = \partial_x - \lambda$.
		\textbf{Solution fondamentale :} $E(x) = e^{\lambda x}H(x)$.
		
		\medskip
		\textbf{Vérification} (formule des sauts + règle de Leibniz) :
		\[
		E' - \lambda E
		= \bigl(e^{\lambda x}H\bigr)' - \lambda e^{\lambda x}H
		= \lambda e^{\lambda x}H + e^{\lambda \cdot 0}\,\delta_0 - \lambda e^{\lambda x}H
		= \delta_0. \quad\checkmark
		\]
		
		\textbf{Interprétation.} $E(x)$ est nulle pour $x < 0$ (avant l'impulsion) et
		exponentielle pour $x > 0$ — c'est la \emph{réponse causale} du système.
	\end{example}
	
	\begin{example}[Laplacien en dimension $d \geq 2$ : $-\Delta u = f$]
		\textbf{Solutions fondamentales :}
		\[
		E(x) = \frac{1}{2\pi}\ln|x| \qquad (d = 2),
		\qquad
		E(x) = \frac{-1}{(d-2)\,S_{d-1}\,|x|^{d-2}} \qquad (d \geq 3),
		\]
		où $S_{d-1}$ est l'aire de la sphère unité $S^{d-1}$. En dimension $3$ :
		\[
		E(x) = \frac{1}{4\pi|x|}.
		\]
		
		\textbf{Interprétation physique.} C'est le \textbf{potentiel de Coulomb} : le
		potentiel électrique créé par une charge ponctuelle unitaire. La théorie des
		distributions formalise ce que les physiciens utilisaient intuitivement.
		
		\medskip
		\textbf{Régularité.} Ces solutions sont $C^\infty$ hors de $0$, donc toute
		solution de $-\Delta u = f$ avec $f \in C^\infty$ est aussi $C^\infty$
		(régularité elliptique — conséquence du Théorème 7.3\,(ii)).
	\end{example}
	
	\begin{example}[Équation des ondes en dimension $1+1$ : $\partial_t^2 u - \partial_x^2 u = f$]
		\textbf{Solution fondamentale :}
		\[
		E(x,t) = \tfrac{1}{2}\,H(t - |x|)
		= \begin{cases} \tfrac{1}{2} & \text{si } t > |x|, \\ 0 & \text{sinon.} \end{cases}
		\]
		
		\textbf{Interprétation.} $E$ est nulle hors du cône $\{t > |x|\}$ et vaut $\tfrac{1}{2}$
		à l'intérieur. L'information se propage exactement à la vitesse $1$ dans les deux
		directions — c'est le \textbf{principe de Huygens} en dimension $1$.
		
		\medskip
		\textbf{Vérification.} Pour $\varphi \in \mathcal{D}(\mathbb{R}^2)$, un calcul direct par
		Fubini et intégrations par parties donne
		$\langle (\partial_t^2 - \partial_x^2)E, \varphi \rangle = \varphi(0,0)$.
	\end{example}
	
	%----------------------------------------------------------
	\begin{qcmblock}
		\hypertarget{qcm:ch7:start}{}\label{qcm:ch7:start}
		\hypertarget{qcm:ch7:q1}{}\label{qcm:ch7:q1}
		\textbf{Question 1.} Laquelle des affirmations suivantes est vraie ?
		\begin{enumerate}[label=\Alph*., itemsep=2pt]
			\item $E$ est toujours $C^\infty$ sur $\mathbb{R}^d$ entier.
			\item $E$ vérifie $P(\partial)E = \delta_0$.
			\item $E$ est unique pour tout opérateur $P(\partial)$.
			\item $E$ n'existe que si $P(\partial)$ est d'ordre $1$.
		\end{enumerate}
		\navtoQCMRep{ch7}{1}
		
		\medskip
		\hypertarget{qcm:ch7:q2}{}\label{qcm:ch7:q2}
		\textbf{Question 2.} Soit $E$ une solution fondamentale de $P(\partial)$ et
		$F \in \mathcal{D}'(\mathbb{R}^d)$ à support compact. Quelle expression est une solution
		de $P(\partial)u = F$ ?
		\begin{enumerate}[label=\Alph*., itemsep=2pt]
			\item $u = E + F$
			\item $u = P(\partial)F$
			\item $u = E * F$
			\item $u = F * F$
		\end{enumerate}
		\navtoQCMRep{ch7}{2}
		
		\medskip
		\hypertarget{qcm:ch7:q3}{}\label{qcm:ch7:q3}
		\textbf{Question 3.} Quelle est la solution fondamentale du Laplacien $-\Delta$
		en dimension $3$ ?
		\begin{enumerate}[label=\Alph*., itemsep=2pt]
			\item $E(x) = \dfrac{\ln|x|}{2\pi}$
			\item $E(x) = \dfrac{1}{4\pi|x|}$
			\item $E(x) = e^{-|x|}$
			\item $E(x) = |x|$
		\end{enumerate}
		\navtoQCMRep{ch7}{3}
	\end{qcmblock}
	%----------------------------------------------------------
	\begin{exerciceblock}{Calculer des solutions fondamentales}
		\hypertarget{exo:ch7:ex1}{}\label{exo:ch7:ex1}
		
		\begin{enumerate}[itemsep=5pt]
			\item Montrer que $E(x) = e^{\lambda x}H(x)$ est une solution fondamentale
			de l'opérateur $P(\partial) = \partial_x - \lambda$,
			c'est-à-dire que $E' - \lambda E = \delta_0$ dans $\mathcal{D}'(\mathbb{R})$.
			\item En déduire qu'une solution particulière de $u' - \lambda u = g$
			(avec $g \in L^1(\mathbb{R})$ à support compact) est $u = E * g$.
			Écrire explicitement cette convolution.
			\item Vérifier que $(T_{|x|})'' = 2\delta_0$ en appliquant deux fois
			la formule des sauts.
		\end{enumerate}
	\end{exerciceblock}
	\navtoCorr{ch7}{1}
	
	%----------------------------------------------------------
	\begin{retenir}{Chapitre 7}
		\begin{itemize}
			\item La \textbf{solution fondamentale} $E$ vérifie $P(\partial)E = \delta_0$
			et existe toujours (Malgrange–Ehrenpreis 1955).
			\item \textbf{Résolution par convolution :} $u = E * F$ résout $P(\partial)u = F$
			(pour $F$ à support compact).
			\item Résultats clés :
			$H' = \delta_0$,\quad
			$E_{\text{transport}}(x) = e^{\lambda x}H(x)$,\quad
			$E_{-\Delta}(x) = \dfrac{1}{4\pi|x|}$ (dim.\,3),\quad
			$E_{\text{ondes}}(x,t) = \tfrac{1}{2}H(t-|x|)$.
			\item La dérivée au sens des distributions \textbf{prolonge} la dérivée classique :
			si $f$ est $C^1$, alors $T_{f'} = (T_f)'$.
		\end{itemize}
	\end{retenir}
	
	
	
	
	% ============================================================
	% ANNEXE : CORRECTIONS DES EXERCICES ET RÉPONSES AUX QCM
	% ============================================================
	\appendix
	\chapter*{Corrections des exercices et réponses aux QCM}
	\addcontentsline{toc}{chapter}{Corrections des exercices et QCM}
	\markboth{CORRECTIONS}{CORRECTIONS}
	
	% ============================================================
	% RÉPONSES AUX QCM
	% ============================================================
	\section*{Réponses aux QCM}
	\addcontentsline{toc}{section}{Réponses aux QCM}
	\markboth{RÉPONSES AUX QCM}{RÉPONSES AUX QCM}
	
	% --- Chapitre 1 ---
	\subsection*{Chapitre 1 — Notion de distribution}
	\navtoQCMchap{1}
	
	\hypertarget{qcmrep:ch1:q1}{}\label{qcmrep:ch1:q1}
	\textbf{Réponse 1 :} B. Toute fonction localement intégrable est une distribution.
	
	\medskip
	\hypertarget{qcmrep:ch1:q2}{}\label{qcmrep:ch1:q2}
	\textbf{Réponse 2 :} B. $-3$ (car $\langle \delta_2, \varphi \rangle = \varphi(2) = -3$).
	
	% --- Chapitre 2 ---
	\subsection*{Chapitre 2 — Convergence dans $\mathcal{D}'(\Omega)$}
	\navtoQCMchap{2}
	
	\hypertarget{qcmrep:ch2:q1}{}\label{qcmrep:ch2:q1}
	\textbf{Réponse 1 :} B. Pour toute $\varphi \in \mathcal{D}(\Omega)$, $\langle T_n, \varphi \rangle \to \langle T, \varphi \rangle$ dans $\mathbb{R}$.
	
	\medskip
	\hypertarget{qcmrep:ch2:q2}{}\label{qcmrep:ch2:q2}
	\textbf{Réponse 2 :} A. $T_n' \xrightarrow{\mathcal{D}'} T'$ pour tout ordre de dérivation.
	
	% --- Chapitre 3 ---
	\subsection*{Chapitre 3 — Dérivation des distributions}
	\navtoQCMchap{3}
	
	\hypertarget{qcmrep:ch3:q1}{}\label{qcmrep:ch3:q1}
	\textbf{Réponse 1 :} A. $H(x)$.
	
	\hypertarget{qcmrep:ch3:q2}{}\label{qcmrep:ch3:q2}
	\textbf{Réponse 2 :} B. $-\varphi^{(3)}(0)$.
	
	\hypertarget{qcmrep:ch3:q3}{}\label{qcmrep:ch3:q3}
	\textbf{Réponse 3 :} A. $6|x|$.
	
	\hypertarget{qcmrep:ch3:q4}{}\label{qcmrep:ch3:q4}
	\textbf{Réponse 4 :} B. $-2\delta_0'$.
	
	\hypertarget{qcmrep:ch3:q5}{}\label{qcmrep:ch3:q5}
	\textbf{Réponse 5 :} C. $\delta_0 - \delta_1$.
	
	\hypertarget{qcmrep:ch3:q6}{}\label{qcmrep:ch3:q6}
	\textbf{Réponse 6 :} A. $e^{|x|}\mathrm{sgn}(x)$.
	
	\hypertarget{qcmrep:ch3:q7}{}\label{qcmrep:ch3:q7}
	\textbf{Réponse 7 :} A. $\displaystyle\sum_{n\in\mathbb{Z}}\delta_n'$.
	
	\hypertarget{qcmrep:ch3:q8}{}\label{qcmrep:ch3:q8}
	\textbf{Réponse 8 :} D. Si $f \in C^0(\mathbb{R})$ et $f$ n'est pas $C^1$, alors $f'$ n'existe pas dans $\mathcal{D}'(\mathbb{R})$. (Faux)
	
	\hypertarget{qcmrep:ch3:q9}{}\label{qcmrep:ch3:q9}
	\textbf{Réponse 9 :} A. $-\mathrm{Pf}\dfrac{1}{x^2}$.
	
	\hypertarget{qcmrep:ch3:q10}{}\label{qcmrep:ch3:q10}
	\textbf{Réponse 10 :} B. une combinaison finie de $\delta_0, \delta_0', \delta_0'', \ldots$
	
	% --- Chapitre 4 ---
	\subsection*{Chapitre 4 — Primitives des distributions}
	\navtoQCMchap{4}
	
	\hypertarget{qcmrep:ch4:q1}{}\label{qcmrep:ch4:q1}\textbf{Réponse 1 :} B. $H(x)$.
	
	\hypertarget{qcmrep:ch4:q2}{}\label{qcmrep:ch4:q2}\textbf{Réponse 2 :} C. $T = C$ (constante).
	
	\hypertarget{qcmrep:ch4:q3}{}\label{qcmrep:ch4:q3}\textbf{Réponse 3 :} B. $T = Cx + D$.
	
	\hypertarget{qcmrep:ch4:q4}{}\label{qcmrep:ch4:q4}\textbf{Réponse 4 :} A. $2H(x)$.
	
	\hypertarget{qcmrep:ch4:q5}{}\label{qcmrep:ch4:q5}\textbf{Réponse 5 :} B. $\delta_0^{(n-1)}$.
	
	\hypertarget{qcmrep:ch4:q6}{}\label{qcmrep:ch4:q6}\textbf{Réponse 6 :} C. Une infinité.
	
	\hypertarget{qcmrep:ch4:q7}{}\label{qcmrep:ch4:q7}\textbf{Réponse 7 :} B. $|x| + C$.
	
	\hypertarget{qcmrep:ch4:q8}{}\label{qcmrep:ch4:q8}\textbf{Réponse 8 :} A. $2H(x)$.
	
	\hypertarget{qcmrep:ch4:q9}{}\label{qcmrep:ch4:q9}\textbf{Réponse 9 :} C. $\mathrm{vp}\dfrac{1}{x}$.
	
	\hypertarget{qcmrep:ch4:q10}{}\label{qcmrep:ch4:q10}\textbf{Réponse 10 :} B. $T = xH(x) + Cx + D$.
	
	% --- Chapitre 5 ---
	\subsection*{Chapitre 5 — Opérations sur les distributions}
	\navtoQCMchap{5}
	
	\hypertarget{qcmrep:ch5:q1}{}\label{qcmrep:ch5:q1}\textbf{Réponse 1 :} A. $\langle T, a\varphi \rangle$.
	
	\hypertarget{qcmrep:ch5:q2}{}\label{qcmrep:ch5:q2}\textbf{Réponse 2 :} C. $0$.
	
	\hypertarget{qcmrep:ch5:q3}{}\label{qcmrep:ch5:q3}\textbf{Réponse 3 :} B. $T = C\delta_0$, $C \in \mathbb{C}$.
	
	\hypertarget{qcmrep:ch5:q4}{}\label{qcmrep:ch5:q4}\textbf{Réponse 4 :} C. $T = 5x + C$, $C \in \mathbb{C}$.
	
	\hypertarget{qcmrep:ch5:q5}{}\label{qcmrep:ch5:q5}\textbf{Réponse 5 :} B. $\mathrm{sgn}(x-1)$.
	
	\hypertarget{qcmrep:ch5:q6}{}\label{qcmrep:ch5:q6}\textbf{Réponse 6 :} A. $\delta_0 - \delta_1$.
	
	\hypertarget{qcmrep:ch5:q7}{}\label{qcmrep:ch5:q7}\textbf{Réponse 7 :} B. $a \cdot T$ avec $a \in C^\infty$.
	
	\hypertarget{qcmrep:ch5:q8}{}\label{qcmrep:ch5:q8}\textbf{Réponse 8 :} B. $e^x H(x) + \delta_0$.
	
	\hypertarget{qcmrep:ch5:q9}{}\label{qcmrep:ch5:q9}\textbf{Réponse 9 :} C. $m+1$.
	
	\hypertarget{qcmrep:ch5:q10}{}\label{qcmrep:ch5:q10}\textbf{Réponse 10 :} B. $T$ est affine : $T = ax + b$.
	
	% --- Chapitre 6 ---
	\subsection*{Chapitre 6 — Convolution des distributions}
	\navtoQCMchap{6}
	
	\hypertarget{qcmrep:ch6:q1}{}\label{qcmrep:ch6:q1}\textbf{Réponse 1 :} C. $\delta_0$.
	
	\hypertarget{qcmrep:ch6:q2}{}\label{qcmrep:ch6:q2}\textbf{Réponse 2 :} B. Non, il faut une hypothèse sur les supports.
	
	\hypertarget{qcmrep:ch6:q3}{}\label{qcmrep:ch6:q3}\textbf{Réponse 3 :} B. $T'$.
	
	\hypertarget{qcmrep:ch6:q4}{}\label{qcmrep:ch6:q4}\textbf{Réponse 4 :} C. $xH(x)$.
	
	\hypertarget{qcmrep:ch6:q5}{}\label{qcmrep:ch6:q5}\textbf{Réponse 5 :} A. $\partial^\alpha(T*S) = (\partial^\alpha T)*S = T*(\partial^\alpha S)$.
	
	\hypertarget{qcmrep:ch6:q6}{}\label{qcmrep:ch6:q6}\textbf{Réponse 6 :} B. $C_0^\infty(\mathbb{R}^d)$ est dense dans $\mathcal{D}'$ pour la topologie des distributions.
	
	\hypertarget{qcmrep:ch6:q7}{}\label{qcmrep:ch6:q7}\textbf{Réponse 7 :} A. $\delta_0^{(n+m)}$.
	
	\hypertarget{qcmrep:ch6:q8}{}\label{qcmrep:ch6:q8}\textbf{Réponse 8 :} B. $H(x-1)$.
	
	\hypertarget{qcmrep:ch6:q9}{}\label{qcmrep:ch6:q9}\textbf{Réponse 9 :} B. La réponse impulsionnelle d'un système linéaire invariant par translation.
	
	\hypertarget{qcmrep:ch6:q10}{}\label{qcmrep:ch6:q10}\textbf{Réponse 10 :} C. $\delta_0$.
	
	% --- Chapitre 7 ---
	\subsection*{Chapitre 7 — Applications aux EDP linéaires}
	\navtoQCMchap{7}
	
	\hypertarget{qcmrep:ch7:q1}{}\label{qcmrep:ch7:q1}\textbf{Réponse 1 :} B. $E$ vérifie $P(\partial)E = \delta_0$.
	
	\hypertarget{qcmrep:ch7:q2}{}\label{qcmrep:ch7:q2}\textbf{Réponse 2 :} C. $u = E * F$.
	
	\hypertarget{qcmrep:ch7:q3}{}\label{qcmrep:ch7:q3}\textbf{Réponse 3 :} B. $\dfrac{1}{4\pi|x|}$.
	
	\vspace{1cm}
	% ============================================================
	% CORRECTIONS DES EXERCICES
	% ============================================================
	\section*{Corrections des exercices}
	\addcontentsline{toc}{section}{Corrections des exercices}
	\markboth{CORRECTIONS DES EXERCICES}{CORRECTIONS DES EXERCICES}
	
	\subsection*{Chapitre 1 — Notion de distribution}
	
	\hypertarget{corr:ch1:ex1}{}\label{corr:ch1:ex1}
	\navtoExo{ch1}{1}
	
	\begin{correctionblock}{Vérifier qu'une forme est une distribution}
		\begin{enumerate}
			\item \textbf{Linéarité.} Pour $\lambda, \mu \in \mathbb{R}$ et $\varphi, \psi \in \mathcal{D}(\mathbb{R})$ :
			\begin{align*}
				\langle T, \lambda \varphi + \mu \psi \rangle
				&= 3(\lambda \varphi + \mu \psi)(0) - (\lambda \varphi + \mu \psi)(1) \\
				&= \lambda\bigl(3\varphi(0)-\varphi(1)\bigr) + \mu\bigl(3\psi(0)-\psi(1)\bigr) \\
				&= \lambda\langle T,\varphi\rangle + \mu\langle T,\psi\rangle.
			\end{align*}
			\item \textbf{Majoration.} Soit $K \subset \mathbb{R}$ compact avec $\operatorname{supp}(\varphi) \subset K$. Alors :
			\[
			|\langle T, \varphi \rangle| = |3\varphi(0) - \varphi(1)| \le 3|\varphi(0)| + |\varphi(1)| \le 4 \sup_{x \in K} |\varphi(x)|.
			\]
			C'est bien la majoration avec $C_K = 4$ et $m = 0$. Donc $T \in \mathcal{D}'(\mathbb{R})$. $\checkmark$
			\item La majoration tient avec $m = 0$ (aucune dérivée de $\varphi$ n'intervient) : $T$ est d'ordre $0$.
			\item On reconnaît directement :
			\[
			\langle T, \varphi \rangle = 3\langle \delta_0, \varphi \rangle - \langle \delta_1, \varphi \rangle = \langle 3\delta_0 - \delta_1, \varphi \rangle, \quad \text{donc } T = 3\delta_0 - \delta_1.
			\]
		\end{enumerate}
	\end{correctionblock}
	
	% ==== CHAPITRE 2 ====
	\section*{Chapitre 2 — Convergence dans $\mathcal{D}'(\Omega)$}
	
	\subsection*{Convergence vers le Dirac}
	
	\hypertarget{corr:ch2:ex1}{}\label{corr:ch2:ex1}
	\navtoExo{ch2}{1}
	
	\begin{correctionblock}{Convergence vers le Dirac}
		\begin{enumerate}
			\item Par le changement de variable $t = nx$ :
			\[
			\int_{-\infty}^{+\infty} \frac{n}{\pi(1+n^2 x^2)} \, dx = \int_{-\infty}^{+\infty} \frac{1}{\pi(1+t^2)} \, dt = \frac{1}{\pi} \bigl[\arctan(t)\bigr]_{-\infty}^{+\infty} = \frac{1}{\pi} \cdot \pi = 1.
			\]
			\item Soit $\varphi \in \mathcal{D}(\mathbb{R})$. Avec $t = nx$ :
			\[
			\langle T_{h_n}, \varphi \rangle = \int_{-\infty}^{+\infty} \frac{n}{\pi(1+n^2 x^2)} \varphi(x) \, dx = \int_{-\infty}^{+\infty} \frac{1}{\pi(1+t^2)} \varphi\!\left(\frac{t}{n}\right) dt.
			\]
			Comme $\varphi$ est continue et bornée, et $\varphi(t/n) \to \varphi(0)$ pour tout $t$, le théorème de convergence dominée (avec $\frac{1}{1+t^2}$ intégrable) donne :
			\[
			\langle T_{h_n}, \varphi \rangle \to \varphi(0) \int_{-\infty}^{+\infty} \frac{dt}{\pi(1+t^2)} = \varphi(0) = \langle \delta_0, \varphi \rangle.
			\]
			Donc $h_n \to \delta_0$.
		\end{enumerate}
	\end{correctionblock}
	
	% ==== CHAPITRE 3 ====
	\section*{Chapitre 3 — Dérivation des distributions}
	
	\subsection*{Exercice 1 — Dérivation de fonctions par morceaux}
	
	\hypertarget{corr:ch3:ex1}{}\label{corr:ch3:ex1}
	\navtoExo{ch3}{1}
	
	\begin{correctionblock}{Dérivation de fonctions par morceaux}
		\emph{Sauts.} $f$ est continue en $0$ (les deux membres valent $0$) et continue en $1$ (les deux membres valent $1$). Donc $[f]_0 = [f]_1 = 0$.
		
		La dérivée classique par morceaux est $[f'](x) = 0$ si $x < 0$, $[f'](x) = 2x$ si $0 < x < 1$, $[f'](x) = -1$ si $x > 1$.
		Les sauts de $f'$ : en $0$, $[f']_0 = 2 \cdot 0 - 0 = 0$ ; en $1$, $[f']_1 = -1 - 2 = -3$.
		
		Donc par la formule des sauts :
		\[
		f' = [f'] = \begin{cases}0 & x < 0,\\ 2x & 0 < x < 1,\\ -1 & x > 1,\end{cases}
		\qquad
		f'' = [f''] + [f']_1\,\delta_1 = \begin{cases}0 & x < 0,\\ 2 & 0 < x < 1,\\ 0 & x > 1,\end{cases} - 3\,\delta_1.
		\]
		On a $[f'']_0 = 2 - 0 = 2$, d'où
		\[
		f'' = 2\,\mathbf{1}_{[0,1]} - 3\,\delta_1, \qquad
		f''' = 2(\delta_0 - \delta_1) - 3\,\delta_1' = 2\delta_0 - 2\delta_1 - 3\delta_1'.
		\]
	\end{correctionblock}
	
	\subsection*{Exercice 2 — Action de $x\,\delta_0^{(n)}$}
	
	\hypertarget{corr:ch3:ex2}{}\label{corr:ch3:ex2}
	\navtoExo{ch3}{2}
	
	\begin{correctionblock}{Action de $x\,\delta_0^{(n)}$}
		\begin{align*}
			\langle x^2\delta_0'', \varphi\rangle &= \langle \delta_0'', x^2\varphi\rangle = (x^2\varphi)''(0).
		\end{align*}
		Or $(x^2\varphi)'' = 2\varphi + 4x\varphi' + x^2\varphi''$, donc $(x^2\varphi)''|_{x=0} = 2\varphi(0)$. D'où $x^2\delta_0'' = 2\delta_0$.
		
		De même, $\langle x^3\delta_0'', \varphi\rangle = (x^3\varphi)''(0)$. Or $(x^3\varphi)'' = 6x\varphi + 6x^2\varphi' + x^3\varphi''$, qui s'annule en $0$. Donc $x^3\delta_0'' = 0$.
	\end{correctionblock}
	
	\subsection*{Exercice 3 — Dérivée de $|x|\sin x$}
	
	\hypertarget{corr:ch3:ex3}{}\label{corr:ch3:ex3}
	\navtoExo{ch3}{3}
	
	\begin{correctionblock}{Dérivée de $|x|\sin x$}
		$|x|\sin x$ est continue donc ses dérivées jusqu'à l'ordre où elle reste $C^k$ ne font pas apparaître de Dirac.
		
		$T' = |x|'\sin x + |x|\cos x = \mathrm{sgn}(x)\sin x + |x|\cos x$.
		
		Pour $T''$, on dérive encore. $\mathrm{sgn}(x)$ a la dérivée $2\delta_0$ :
		\[
		T'' = 2\delta_0 \cdot \sin(0) + \mathrm{sgn}(x)\cos x + \mathrm{sgn}(x)\cos x - |x|\sin x = 2\mathrm{sgn}(x)\cos x - |x|\sin x.
		\]
		(Le terme en $\delta_0$ disparaît car $\sin(0) = 0$.)
	\end{correctionblock}
	
	\subsection*{Exercice 4 — Vérification par deux méthodes}
	
	\hypertarget{corr:ch3:ex4}{}\label{corr:ch3:ex4}
	\navtoExo{ch3}{4}
	
	\begin{correctionblock}{Vérification par deux méthodes}
		\textbf{Méthode 1 (définition directe).}
		\[
		\langle(H\sin x)', \varphi\rangle = -\int_0^{+\infty}\sin(x)\varphi'(x)\,dx = \varphi(0) + \int_0^{+\infty}\cos(x)\varphi(x)\,dx
		\]
		par IPP (le crochet vaut $[-\sin(x)\varphi(x)]_0^{+\infty} = 0$ et $\int_0^\infty \cos(x)\varphi(x)\,dx$). D'où $\varphi(0) + \langle H\cos x, \varphi\rangle$.
		
		\textbf{Méthode 2 (Leibniz).}
		$(H\sin x)' = H'\sin x + H\cos x = \delta_0 \cdot \sin(0) + H\cos x = 0 + H\cos x$.
		
		Attention : $\delta_0 \cdot \sin(x)$ vaut $\sin(0)\delta_0 = 0$. Les deux méthodes donnent le même résultat.
	\end{correctionblock}
	
	\subsection*{Exercice 5 — Dérivée du peigne de Dirac et série trigonométrique}
	
	\hypertarget{corr:ch3:ex5}{}\label{corr:ch3:ex5}
	\navtoExo{ch3}{5}
	
	\begin{correctionblock}{Dérivée du peigne de Dirac et série trigonométrique}
		La dérivation commute avec les séries convergentes dans $\mathcal{D}'$ :
		\[
		\Delta_a' = \left(\sum_{n\in\mathbb{Z}}\delta_{na}\right)' = \sum_{n\in\mathbb{Z}}\delta_{na}'.
		\]
		On sait (cours, section 4.4.5) que $\displaystyle\sum_{k=1}^{+\infty}\cos(kx) = -\tfrac{1}{2} + \pi\sum_{m\in\mathbb{Z}}\delta_{2m\pi}$.
		En dérivant membre à membre :
		\[
		-\sum_{k=1}^{+\infty} k\sin(kx) = \pi\sum_{m\in\mathbb{Z}}\delta_{2m\pi}',
		\]
		soit $\displaystyle\sum_{n=1}^{+\infty} n\sin(nx) = -\pi\sum_{k\in\mathbb{Z}}\delta_{2k\pi}'$.
	\end{correctionblock}
	
	% ==== CHAPITRE 4 ====
	\section*{Chapitre 4 — Primitives des distributions}
	
	\subsection*{Exercice 1 — Primitives successives de $\delta_0$}
	
	\hypertarget{corr:ch4:ex1}{}\label{corr:ch4:ex1}
	\navtoExo{ch4}{1}
	
	\begin{correctionblock}{Primitives successives de $\delta_0$}
		\begin{itemize}
			\item Ordre 1 : $T_1 = H(x)$. Vérification : $H' = \delta_0$, et $H(x) = 0$ pour $x < 0$. \checkmark
			\item Ordre 2 : $T_2 = xH(x)$. Vérification : $T_2' = H(x) + x\delta_0 = H(x)$, et $T_2'' = \delta_0$. Et $T_2(x) = 0$ pour $x < 0$. \checkmark
			\item Ordre 3 : $T_3 = \dfrac{x^2}{2}H(x)$.
			$T_3' = xH(x) + \dfrac{x^2}{2}\delta_0 = xH(x)$, $T_3'' = H(x)$, $T_3''' = \delta_0$. \checkmark
		\end{itemize}
		En général : $T_n = \dfrac{x^n}{n!}H(x)$ pour $n \geq 0$.
	\end{correctionblock}
	
	\subsection*{Exercice 2 — Résolution de $T' = H - 1$}
	
	\hypertarget{corr:ch4:ex2}{}\label{corr:ch4:ex2}
	\navtoExo{ch4}{2}
	
	\begin{correctionblock}{Résolution de $T' = H - 1$}
		On cherche une primitive particulière de $f(x) = H(x) - 1$.
		$f(x) = \begin{cases}-1 & x < 0,\\ 0 & x > 0.\end{cases}$
		Une primitive (au sens classique, donc aussi distributionnel) est $F(x) = \int_0^x f(t)\,dt = \begin{cases}-x & x < 0,\\ 0 & x > 0.\end{cases} = -x\,H(-x)$.
		
		On peut aussi écrire $F(x) = -x + xH(x) = x(H(x) - 1)$.
		Vérification : $F'(x) = H(x) - 1 + x\delta_0 = H(x) - 1$. \checkmark
		
		Solution générale : $T = -xH(-x) + C$.
	\end{correctionblock}
	
	\subsection*{Exercice 3 — Équation de la chaleur et primitive en $t$}
	
	\hypertarget{corr:ch4:ex3}{}\label{corr:ch4:ex3}
	\navtoExo{ch4}{3}
	
	\begin{correctionblock}{Équation de la chaleur et primitive en $t$}
		\emph{Convergence vers $\delta_0$.} Pour $\varphi \in \mathcal{D}(\mathbb{R})$, par le changement $u = x/(2\sqrt{t})$ :
		\[
		\int_{-\infty}^{+\infty}\Gamma(x,t)\varphi(x)\,dx = \frac{1}{\sqrt{\pi}}\int_{-\infty}^{+\infty}e^{-u^2}\varphi(2u\sqrt{t})\,du \xrightarrow{t\to 0^+} \frac{\varphi(0)}{\sqrt{\pi}}\int_{-\infty}^{+\infty}e^{-u^2}\,du = \varphi(0).
		\]
		Convergence dominée justifiée car $|\varphi(2u\sqrt{t})| \leq \|\varphi\|_\infty$ et $e^{-u^2}$ est intégrable.
		
		\emph{Équation de la chaleur.} Pour $t > 0$, $\Gamma$ vérifie $\partial_t\Gamma = \partial_x^2\Gamma$ (calcul classique). Donc
		\[
		\partial_t E - \partial_x^2 E = \Gamma(x,0^+)\delta_0(t) = \delta_0(x)\delta_0(t) = \delta_{(0,0)}.
		\]
		(En utilisant la formule des sauts en $t = 0$ et la convergence $\Gamma(\cdot,t) \to \delta_0$.)
	\end{correctionblock}
	
	\subsection*{Exercice 4 — Primitives de la valeur principale}
	
	\hypertarget{corr:ch4:ex4}{}\label{corr:ch4:ex4}
	\navtoExo{ch4}{4}
	
	\begin{correctionblock}{Primitives de la valeur principale}
		\[
		\left\langle x\,\mathrm{vp}\frac{1}{x}, \varphi\right\rangle = \left\langle \mathrm{vp}\frac{1}{x}, x\varphi\right\rangle = \lim_{\varepsilon\to 0^+}\int_{|t|\geq\varepsilon}\frac{t\varphi(t)}{t}\,dt = \lim_{\varepsilon\to 0^+}\int_{|t|\geq\varepsilon}\varphi(t)\,dt = \int_{-\infty}^{+\infty}\varphi(t)\,dt = \langle 1, \varphi\rangle.
		\]
		Donc $x\,\mathrm{vp}\frac{1}{x} = 1$. La primitive de $1$ (fonction constante) est $x$ (classiquement), ce qui se vérifie aisément dans $\mathcal{D}'$.
	\end{correctionblock}
	
	\subsection*{Exercice 5 — Structure des primitives du peigne de Dirac}
	
	\hypertarget{corr:ch4:ex5}{}\label{corr:ch4:ex5}
	\navtoExo{ch4}{5}
	
	\begin{correctionblock}{Structure des primitives du peigne de Dirac}
		Puisque $H(x-n)' = \delta_n$ pour tout $n \in \mathbb{Z}$, et que la dérivée commute avec les séries convergentes dans $\mathcal{D}'$, on a
		\[
		\Sigma(x) = \sum_{n\in\mathbb{Z}} H(x - n).
		\]
		Pour $x \in [k, k+1)$ avec $k \in \mathbb{Z}$, la somme vaut $k+1$ (les termes $H(x-n)$ avec $n \leq k$ valent $1$, les autres $0$). Donc $\Sigma(x) = \lfloor x \rfloor + 1$ pour $x \geq 0$.
		
		Vérification : $\lfloor x \rfloor' = \Delta_1$. \checkmark
	\end{correctionblock}
	
	% ==== CHAPITRE 5 ====
	\section*{Chapitre 5 — Opérations sur les distributions}
	
	\subsection*{Exercice 1 — Calcul de dérivées par la définition}
	
	\hypertarget{corr:ch5:ex1}{}\label{corr:ch5:ex1}
	\navtoExo{ch5}{1}
	
	\begin{correctionblock}{Calcul de dérivées par la définition}
		\textbf{(a)} Pour $\varphi \in \mathcal{D}(\mathbb{R})$ :
		\[
		\langle T_{\mathrm{sgn}}', \varphi \rangle = -\langle T_{\mathrm{sgn}}, \varphi' \rangle = -\int_{-\infty}^0 (-\varphi')\,dx - \int_0^{+\infty} \varphi'\,dx = \varphi(0) + \varphi(0) = 2\varphi(0).
		\]
		Donc $\mathrm{sgn}'(x) = 2\delta_0$.
		
		\textbf{(b)} Par intégration par parties :
		\[
		\langle |x|', \varphi \rangle = \int_{-\infty}^0 (-\varphi)\,dx + \int_0^{+\infty} \varphi\,dx = \langle \mathrm{sgn}(x), \varphi \rangle.
		\]
		Donc $|x|' = \mathrm{sgn}(x)$. Consistance : $\mathrm{sgn}'(x) = 2\delta_0 = |x|''$. \checkmark
		
		\textbf{(c)} Utilisons la formule des sauts. La fonction $e^x H(x)$ est continue par morceaux avec un saut en $0$ :
		\[
		(e^x H)^+ - (e^x H)^- = e^0 - 0 = 1.
		\]
		Donc $(e^x H)' = e^x H + 1 \cdot \delta_0 = e^x H(x) + \delta_0$.
	\end{correctionblock}
	
	\subsection*{Exercice 2 — Équations algébriques dans $\mathcal{D}'(\mathbb{R})$}
	
	\hypertarget{corr:ch5:ex2}{}\label{corr:ch5:ex2}
	\navtoExo{ch5}{2}
	
	\begin{correctionblock}{Équations algébriques dans $\mathcal{D}'(\mathbb{R})$}
		\textbf{(a)} D'après la proposition fondamentale : $T = C\delta_0$, $C \in \mathbb{C}$.
		
		\textbf{(b)} Comme $x\,\mathrm{vp}\frac{1}{x} = 1$, l'équation s'écrit $x(T - \mathrm{vp}\frac{1}{x}) = 0$. Donc $T = \mathrm{vp}\frac{1}{x} + C\delta_0$.
		
		\textbf{(c)} Posons $S = xT$. L'équation $x^2 T = \delta_0$ devient $x S = \delta_0$. On sait que $x(-\delta_0') = -x\delta_0' = -(x\delta_0)' + \delta_0 = 0 + \delta_0 = \delta_0$ (utilisant $x\delta_0=0$ et la règle de Leibniz). Donc $S_0 = -\delta_0'$ est solution particulière, et $S = -\delta_0' + c_1\delta_0$.
		
		On résout ensuite $xT = S = -\delta_0' + c_1\delta_0$. Par linéarité et les formules de (b) :
		\[
		T = \frac{1}{2}\delta_0'' + c_1'(-\delta_0') + c_0\delta_0 = \frac{1}{2}\delta_0'' + c_1\delta_0' + c_0\delta_0, \quad c_0, c_1 \in \mathbb{C}.
		\]
	\end{correctionblock}
	
	\subsection*{Exercice 3 — Formule des sauts et dérivées successives}
	
	\hypertarget{corr:ch5:ex3}{}\label{corr:ch5:ex3}
	\navtoExo{ch5}{3}
	
	\begin{correctionblock}{Formule des sauts et dérivées successives}
		La fonction $f$ est continue sur $\mathbb{R}$ (car $\sin(0) = \sin(\pi) = 0$), et de classe $C^1$ sur chaque intervalle $]-\infty,0[$, $]0,\pi[$ et $]\pi,+\infty[$. Elle admet des discontinuités de sa dérivée classique en $0$ et en $\pi$, mais pas de saut en valeur.
		
		\textbf{(a)} Puisque $f$ est continue (aucun saut), la formule des sauts donne :
		\[
		f' = T_{f'_{\mathrm{class}}} = \begin{cases} \cos(x) & x \in (0,\pi), \\ 0 & \text{sinon.}\end{cases} = \cos(x)\,(H(x) - H(x-\pi)).
		\]
		
		\textbf{(b)} La fonction $\cos(x)(H(x)-H(x-\pi))$ présente des sauts en $x=0$ (saut $= 1-0 = 1$) et en $x=\pi$ (saut $= 0 - (-1) = 1$). Donc :
		\[
		f'' = -\sin(x)(H(x)-H(x-\pi)) + 1 \cdot \delta_0 + 1 \cdot \delta_\pi.
		\]
		Comme $-\sin(x)(H(x)-H(x-\pi)) = -f(x)$, on obtient $f'' = -f + \delta_0 + \delta_\pi$.
	\end{correctionblock}
	
	\subsection*{Exercice 4 — Produit et multiplication $C^\infty$}
	
	\hypertarget{corr:ch5:ex4}{}\label{corr:ch5:ex4}
	\navtoExo{ch5}{4}
	
	\begin{correctionblock}{Produit et multiplication $C^\infty$}
		\textbf{(a)} On a $x\,\mathrm{vp}\frac{1}{x} = 1$, donc $x^2\,\mathrm{vp}\frac{1}{x} = x \cdot (x\,\mathrm{vp}\frac{1}{x}) = x \cdot 1 = x$.
		
		\textbf{(b)} \emph{Méthode directe :} $(x^2\,\mathrm{vp}\frac{1}{x})' = x' = 1$ dans $\mathcal{D}'(\mathbb{R})$.
		
		\emph{Méthode Leibniz :} $(x^2\,\mathrm{vp}\frac{1}{x})' = 2x\,\mathrm{vp}\frac{1}{x} + x^2\,(\mathrm{vp}\frac{1}{x})'$. Or $(\ln|x|)' = \mathrm{vp}\frac{1}{x}$, et $(\mathrm{vp}\frac{1}{x})' = -\mathrm{Pf}\frac{1}{x^2}$. Donc :
		\[
		(x^2\,\mathrm{vp}\tfrac{1}{x})' = 2x\,\mathrm{vp}\tfrac{1}{x} - x^2\,\mathrm{Pf}\tfrac{1}{x^2}.
		\]
		On peut vérifier que $x^2\,\mathrm{Pf}\frac{1}{x^2} = 2x\,\mathrm{vp}\frac{1}{x} - 1$ (par un calcul direct), ce qui redonne bien $1$. \checkmark
	\end{correctionblock}
	
	\subsection*{Exercice 5 — Distribution à support réduit à un point}
	
	\hypertarget{corr:ch5:ex5}{}\label{corr:ch5:ex5}
	\navtoExo{ch5}{5}
	
	\begin{correctionblock}{Distribution à support réduit à un point}
		Écrivons $T = \sum_{k=0}^m a_k \delta_0^{(k)}$. Pour $\varphi \in \mathcal{D}(\mathbb{R})$ :
		\[
		\langle x^2 T, \varphi \rangle = \langle T, x^2\varphi \rangle = \sum_{k=0}^m a_k (-1)^k (x^2\varphi)^{(k)}(0).
		\]
		Par la formule de Leibniz, $(x^2\varphi)^{(k)}(0) = 0\cdot\varphi^{(k)}(0) + 2k\varphi^{(k-1)}(0) + k(k-1)\varphi^{(k-2)}(0)$.
		
		En particulier pour $k=0$ et $k=1$ : $(x^2\varphi)^{(0)}(0) = 0$, $(x^2\varphi)^{(1)}(0) = 0$.
		
		L'équation $x^2 T = 0$ implique $a_k = 0$ pour $k \geq 2$ (par test sur des $\varphi$ appropriées). Pour $k=0$ et $k=1$, les termes correspondants sont nuls automatiquement. Donc $T = a_0\delta_0 + a_1\delta_0'$ avec $a_0, a_1$ quelconques.
	\end{correctionblock}
	
	% ==== CHAPITRE 6 ====
	\section*{Chapitre 6 — Convolution des distributions}
	
	\subsection*{Exercice 1 — Convolution et dérivation}
	
	\hypertarget{corr:ch6:ex1}{}\label{corr:ch6:ex1}
	\navtoExo{ch6}{1}
	
	\begin{correctionblock}{Convolution et dérivation}
		\textbf{(a)} Par la propriété de dérivation et le fait que $\delta_0$ est l'élément neutre :
		\[
		\delta_0^{(n)} * H = (\delta_0 * H)^{(n)} = H^{(n)}.
		\]
		Or $H' = \delta_0$, $H'' = \delta_0'$, ..., $H^{(n)} = \delta_0^{(n-1)}$. Donc $\delta_0^{(n)} * H = \delta_0^{(n-1)}$.
		
		\textbf{(b)} Par récurrence. Pour $n=1$ : $H_1 = H$. Pour $n=2$ : $(H*H)(x) = xH(x) = \frac{x^1}{1!}H(x)$. Si le résultat est vrai au rang $n$, alors
		\[
		H^{*(n+1)} = H^{*n} * H = \frac{x^{n-1}}{(n-1)!}H * H.
		\]
		On calcule : $\left(\frac{x^{n-1}}{(n-1)!}H\right) * H = \int_0^x \frac{t^{n-1}}{(n-1)!}\,dt = \frac{x^n}{n!}H(x)$, ce qui achève la récurrence.
	\end{correctionblock}
	
	% ============================================================
	% SUITE DES CORRECTIONS — à coller juste avant \end{document}
% dans main_updated__1_.tex (après la correction Ch6 Ex1)
% ============================================================

\subsection*{Exercice 2 — Convolution avec des translations de Dirac}

\hypertarget{corr:ch6:ex2}{}\label{corr:ch6:ex2}
\navtoExo{ch6}{2}

\begin{correctionblock}{Convolution avec des translations de Dirac}
	Pour tout $\varphi \in \mathcal{D}(\mathbb{R})$ :
	\[
	\langle \delta_a * \delta_b, \varphi \rangle
	= \langle \delta_b, \varphi(a + \cdot) \rangle
	= \langle \delta_b, x \mapsto \varphi(a+x) \rangle
	= \varphi(a+b)
	= \langle \delta_{a+b}, \varphi \rangle.
	\]
	Donc $\delta_a * \delta_b = \delta_{a+b}$.
	
	Pour $\delta_a * f$ : $(\delta_a * f)(x) = \langle \delta_a, f(x - \cdot) \rangle = f(x-a)$.
	Autrement dit, $\delta_a * f = \tau_a f$ est le translaté de $f$ de quantité $a$.
\end{correctionblock}

\subsection*{Exercice 3 — Calcul explicite de produits de convolution}

\hypertarget{corr:ch6:ex3}{}\label{corr:ch6:ex3}
\navtoExo{ch6}{3}

\begin{correctionblock}{Calcul explicite de produits de convolution}
	\textbf{(a)} Posons $f(x) = e^x H(x)$ et $g(x) = H(x)$. Pour $x > 0$ :
	\[
	(f*g)(x) = \int_0^x e^{x-y}\cdot 1\,dy
	= e^x \int_0^x e^{-y}\,dy
	= e^x(1-e^{-x}) = e^x - 1.
	\]
	Pour $x < 0$, les supports ne se chevauchent pas, donc $(f*g)(x) = 0$.
	D'où $e^x H * H = (e^x - 1)H(x)$.
	
	\textbf{(b)} Posons $g(x) = \cos(x)H(x)$. Pour $x > 0$ :
	\[
	(H * g)(x) = \int_0^x \cos(y)\,dy = \sin(x).
	\]
	Pour $x < 0$, le résultat est $0$.
	Donc $H * \cos(x)H(x) = \sin(x)H(x)$.
\end{correctionblock}

\subsection*{Exercice 4 — Convolution et la propriété de régularisation}

\hypertarget{corr:ch6:ex4}{}\label{corr:ch6:ex4}
\navtoExo{ch6}{4}

\begin{correctionblock}{Convolution et la propriété de régularisation}
	\textbf{(a)} Par définition, $(T * \rho_\varepsilon)(x) = \langle T, \rho_\varepsilon(x-\cdot) \rangle$.
	Comme $x \mapsto \rho_\varepsilon(x-y)$ est $C^\infty$ en $x$ et à support compact en $y$
	uniformément en $x$, le théorème de dérivation sous le crochet donne
	$T * \rho_\varepsilon \in C^\infty$.
	
	\textbf{(b)} Pour $\psi \in \mathcal{D}(\mathbb{R})$ :
	\[
	\langle T * \rho_\varepsilon, \psi \rangle = \langle T, \check{\rho}_\varepsilon * \psi \rangle.
	\]
	Comme $\check{\rho}_\varepsilon * \psi \to \psi$ dans $C_0^\infty(\mathbb{R})$ (classique),
	et $T$ est continue sur $C_0^\infty$, on conclut
	$\langle T * \rho_\varepsilon, \psi \rangle \to \langle T, \psi \rangle$.
	
	\textbf{(c)} Pour $f \in L^1_{\mathrm{loc}}$, on applique (b) avec $T = T_f$.
	En fait $f * \rho_\varepsilon \to f$ dans $L^1_{\mathrm{loc}}$ par un argument direct
	de convergence dominée (théorème d'approximation de l'identité).
\end{correctionblock}

\subsection*{Exercice 5 — Convolution et résolution de l'équation $T' - T = \delta_0$}

\hypertarget{corr:ch6:ex5}{}\label{corr:ch6:ex5}
\navtoExo{ch6}{5}

\begin{correctionblock}{Convolution et résolution de l'équation $T' - T = \delta_0$}
	On cherche d'abord une solution élémentaire $E$ vérifiant $E' - E = \delta_0$.
	Posons $E = f(x)H(x)$ avec $f$ à déterminer. Par la formule des sauts :
	\[
	(fH)' = f'H + f(0)\,\delta_0.
	\]
	L'équation devient $f'H + f(0)\,\delta_0 - fH = \delta_0$,
	soit $f(0) = 1$ et $f' - f = 0$ sur $]0, +\infty[$.
	La solution est $f(x) = e^x$ (avec $f(0) = 1$).
	Donc $E(x) = e^x H(x)$.
	
	La solution générale de l'équation homogène $T' - T = 0$
	est $T = Ce^x$ ($C \in \mathbb{C}$).
	
	La solution générale de $T' - T = \delta_0$ est donc :
	\[
	T = e^x H(x) + Ce^x, \quad C \in \mathbb{C}.
	\]
\end{correctionblock}

% ==== CHAPITRE 7 ====
\section*{Chapitre 7 — Applications aux équations différentielles linéaires}

\subsection*{Exercice 1 — Calculer des solutions fondamentales}

\hypertarget{corr:ch7:ex1}{}\label{corr:ch7:ex1}
\navtoExo{ch7}{1}

\begin{correctionblock}{Calculer des solutions fondamentales}
	\textbf{1.} $E$ est $C^1$ par morceaux avec saut $[E(0)] = e^0 = 1$ en $0$.
	Par la formule des sauts :
	\[
	\bigl(e^{\lambda x}H\bigr)'
	= \lambda e^{\lambda x}H + [E(0)]\,\delta_0
	= \lambda e^{\lambda x}H + \delta_0.
	\]
	Donc $E' - \lambda E = \delta_0$. $\checkmark$
	
	\textbf{2.} Par le théorème de résolution par convolution, $u = E * g$ est solution.
	Explicitement :
	\[
	(E * g)(x) = \int_{-\infty}^{x} e^{\lambda(x-y)}\,g(y)\,\mathrm{d}y.
	\]
	C'est la formule de variation de la constante, retrouvée par la théorie des distributions.
	
	\textbf{3.} $f(x) = |x|$ est continue (saut nul en $0$), $C^1$ par morceaux avec
	$f'(x) = \operatorname{sgn}(x)$. La formule des sauts donne $(T_{|x|})' = T_{\operatorname{sgn}}$.
	Puis $g(x) = \operatorname{sgn}(x)$ a un saut $[g(0)] = 1 - (-1) = 2$ en $0$, donc
	$(T_{\operatorname{sgn}})' = 0 + 2\delta_0$.
	Ainsi $(T_{|x|})'' = 2\delta_0$. $\checkmark$
\end{correctionblock}



% ============================================================
% BIBLIOGRAPHIE
% ============================================================
\cleardoublepage
\phantomsection
\addcontentsline{toc}{chapter}{References bibliographiques}
\markboth{References bibliographiques}{REFERENCES BIBLIOGRAPHIQUES}
\renewcommand{\bibname}{Références bibliographiques}

\begin{thebibliography}{99}

\bibitem{Boumaza}
H.~Boumaza, T.~Duyckaerts, E.~Schenck, \textit{Théorie des distributions}. 
Université Sorbonne Paris Nord, 2020. 
\url{https://www.math.univ-paris13.fr/~boumaza/cours/2023-2024-CoursDistribution.pdf}

\bibitem{Hormiere}
P.-J.~Hormière, \textit{Analyse T4, TD n° 6 : Introduction aux distributions}. 
Octobre--novembre 2016. 
\url{https://math.univ-lyon1.fr/~mironescu/resources/maths4_td_6_support.pdf}

\bibitem{MathsCnam}
Maths -- Cnam, \textit{Transformée de Fourier des distributions}.
\url{https://maths.cnam.fr/IMG/pdf/07-11-FourierDistribution.pdf}

% ---- Ouvrage fondateur ----
\bibitem{Schwartz}
L.~Schwartz, \textit{Théorie des distributions}.
Hermann, Paris, 1950--1951 (2 volumes). Nouvelle édition : 1997.

% ---- Manuels français de référence ----
\bibitem{Bony}
J.-M.~Bony, \textit{Cours d'analyse : théorie des distributions et analyse de Fourier}.
Éditions de l'École Polytechnique, Palaiseau, 2001.

\bibitem{Zuily}
C.~Zuily, \textit{Éléments de distributions et d'équations aux dérivées partielles}.
Dunod, Paris, 2002.



\bibitem{FriedlanderJoshi}
F.~G.~Friedlander et M.~S.~Joshi, \textit{Introduction to the Theory of Distributions}.
Cambridge University Press, Cambridge, 2\up{e} édition, 1998.

\bibitem{DuistermaatKolk}
J.~J.~Duistermaat et J.~A.~C.~Kolk, \textit{Distributions: Theory and Applications}.
Birkhäuser, Boston, 2010.



\bibitem{Zemanian}
A.~H.~Zemanian, \textit{Distribution Theory and Transform Analysis}.
Dover Publications, New York, 1987 (réimpression de l'édition de 1965).


\bibitem{Grubb}
G.~Grubb, \textit{Distributions and Operators}.
Graduate Texts in Mathematics, Vol.~252, Springer, New York, 2009.


\end{thebibliography}

\end{document}